---
session_date: 2026-04-23
branch: master
commits: 7
issues_closed: 0
issues_filed: 0
specialist_dispatches: 8
models_used:
  - nano-gpt/zai-org/glm-5
  - openai-codex/gpt-5.3-codex
  - openai-codex/gpt-5.4-mini
  - openai-codex/gpt-5.4
---

# Session Report — 2026-04-23

## Summary

Delivered the full MANDATORY_RULES injection system for specialist prompts. Rules are stored as markdown templates under `config/mandatory-rules/`, loaded at spawn time by `buildMandatoryRulesBlock`, and appended to the end of every task prompt (recency effect, per user decision — overriding overthinker's system-prompt recommendation). Full traceability: a `mandatory_rules_injection` meta event is emitted to events.jsonl and SQLite, surfaced by `sp feed` as `↳ mandatory_rules sets=... rules=N tokens=~N`, and stored in `startup_context` in status.json. Two bugs were found and fixed during E2E verification: (1) the globally installed binary was stale — `npm install -g .` was missing from the merge workflow, causing all previous smoke tests to run against old code; (2) the token cap in runner.ts was checking `(renderedTask + rulesBlock).length / 4 <= 400`, which always exceeded the limit for bead-linked runs — fixed to check only the rules block against a 2000-token ceiling. Additionally fixed a long-standing UX gap in `sp feed`: tool errors were rendering as bare `error` with no message; they now show the first line of `result_summary` (capped at 120 chars), e.g. `read: ENOENT: no such file or directory, access '/some/path'`. Explorer model changed to `nano-gpt/zai-org/glm-5` per user request.

## Issues Closed

*No issues formally closed this session. `unitAI-cdodj` (mandatory-rules impl chain) and `unitAI-gxq8s` (debugger fix for parser fallback) were the primary work items — both reached done state via specialist runs but beads remain open pending formal close.*

## Issues Filed

*No new issues filed this session.*

## Specialist Dispatches

Jobs relevant to this session's work (prior-session carryover shown for context):

| Job | Specialist | Status | Bead | Purpose | Outcome |
|-----|-----------|--------|------|---------|---------|
| 452b7c | executor | done | unitAI-cdodj | Impl: inject MANDATORY_RULES into task prompt | Delivered injection + meta event wiring |
| 6afebf | executor | done | unitAI-olx5y | Fix: metaPayload not passed to timeline mapper | Fixed supervisor→timeline wiring |
| 3aba17 | reviewer | done | unitAI-bfdkw | Review: runner injection + traceability | PARTIAL — triggered follow-up debugger |
| 05ba46 | debugger | done | unitAI-gxq8s | Debug: meta event absent from events.jsonl | Identified body-text parser fallback gap |
| 4fe72e | explorer | waiting | - | Smoke test #1 post-install | Confirmed meta event in events.jsonl ✓ |
| f714bf | explorer | waiting | - | Smoke test #2 post token-cap fix | Confirmed feed rendering works ✓ |
| db6c69 | explorer | waiting | - | Smoke test #3 post fresh install | Confirmed sp feed -f works ✓ |
| 988932 | explorer | waiting | - | Smoke test (pre-fix, stale binary) | Meta event absent — stale binary confirmed |

**Problems with dispatches:**

| Job | Problem | Resolution |
|-----|---------|------------|
| abefd3/d72d58 | Reviewer errored on empty diff (worktree had no changes vs master) | Diagnosed manually; verified fixes directly from main env |
| 988932 | Meta event absent despite parser fix — smoke test ran against stale global binary | Identified via `grep -c "mandatory" $(which sp)` returning 1; fixed with `npm install -g .` |

## Problems Encountered

| Problem | Root Cause | Resolution |
|---------|-----------|------------|
| mandatory_rules meta event never appeared in events.jsonl | Global binary stale — `npm install -g .` not run after `npm run build`; all smoke tests hit old code | Compared `grep -c mandatory $(which sp)` (1) vs `dist/index.js` (28); reinstalled |
| Token cap always skipped injection for bead runs | Guard checked `(renderedTask.length + rulesBlock.length) / 4 <= 400` — renderedTask alone exceeds 400 tokens for bead-linked runs | Changed to `rulesBlock.length / 4 <= 2000` (rules-only check, 30x headroom) |
| sp feed -f showed META line but no `↳ mandatory_rules` sub-line | `sp feed -f` terminal was started before binary update; running process used old binary | Restarted `sp feed -f` with new binary |
| Template files had rules in body text, parser only read frontmatter `rules:` array | mxi9k executor wrote YAML frontmatter parser; ndo7h executor created files with rules as body text | Debugger 05ba46 identified gap; added body-text fallback in `readMandatoryRuleSet` |
| Reviewer blocked on empty worktree diff | All changes committed as checkpoint commits; reviewer diff detection expected uncommitted working tree | Verified fixes directly from main env using `git diff master..feature/...` |

## Code Changes

### Core feature: MANDATORY_RULES injection

**`src/specialist/mandatory-rules.ts`** (new) — Full loader pipeline: `loadMandatoryRulesIndex` reads `config/mandatory-rules/index.json`, `readMandatoryRuleSet` loads template files (user override path first, then config), `parseMandatoryRulesFrontmatter` parses YAML `rules:` arrays, body-text fallback when frontmatter has no rules, `formatMandatoryRulesBlock` renders `## MANDATORY_RULES\n### set\n- [level] text`, `buildMandatoryRulesBlock` orchestrates the full pipeline.

**`src/specialist/runner.ts`** — Import of `buildMandatoryRulesBlock`; `renderedTask` changed from `const` to `let`; rules block appended after template render if non-empty and under 2000-token ceiling; meta event emitted via `onEvent?.('meta', ...)` with `source: 'mandatory_rules_injection'` and full data payload.

**`src/specialist/supervisor.ts`** — `parsedMeta` now handles both `memory_injection` and `meta` event types; `mandatoryRulesInjection` extracted when `parsedMeta.source === 'mandatory_rules_injection'`; stored in `startup_context.mandatory_rules_injection` via `setStatus`; `metaPayload` with `source`/`data` passed into `mapCallbackEventToTimelineEvent`.

**`src/specialist/timeline-events.ts`** — `TimelineEventMeta` extended with `source?: string` and `data?: Record<string, unknown>`; `mapCallbackEventToTimelineEvent` meta case conditionally spreads `source` and `data` from `metaPayload`.

**`src/cli/feed.ts`** — `formatStartupContextLine` handles `event.source === 'mandatory_rules_injection'`; renders `↳ mandatory_rules sets=... rules=N tokens=~N`.

**`src/specialist/schema.ts`** — `MandatoryRuleSchema`, `MandatoryRulesSchema` added; `mandatory_rules` top-level field in `SpecialistSchema`.

**`config/mandatory-rules/`** — 10 template files + `index.json`: `core-session-boundary`, `git-workflow-safe`, `gitnexus-required`, `explorer-readonly`, `executor-delivery`, `reviewer-verdict-format`, `overthinker-4phase`, `debugger-trace-first`, `researcher-source-discipline`, `test-runner-execution-scope`. Rules in body text (not frontmatter).

**`config/specialists/*.specialist.json`** — 8 specialists updated with `mandatory_rules.template_sets`: executor gets delivery+git+gitnexus, explorer gets readonly+gitnexus, reviewer gets verdict-format+gitnexus, etc.

### Bug fix: tool error detail in feed

**`src/cli/format-helpers.ts`** — `formatToolDetail` error branch now shows first line of `result_summary` (capped at 120 chars) instead of bare `error`. Before: `read: error`. After: `read: ENOENT: no such file or directory, access '/path'`.

### Token cap fix

**`src/specialist/runner.ts`** — Cap changed from `(renderedTask.length + rulesBlock.length) / 4 <= 400` to `rulesBlock.length / 4 <= 2000`. Old cap always fired for bead runs. New cap is a true safety guard (current rules ~66 tokens, ceiling is 2000).

## Documentation Updates

No formal doc updates this session. The `config/mandatory-rules/` templates are self-documenting. CHANGELOG not updated — that's part of the 4.0 release pass.

## Open Issues with Context

### Ready for next session

| ID | P | Title | Context / Suggestions |
|----|---|-------|----------------------|
| unitAI-cdodj | P1 | Impl: inject MANDATORY_RULES | Done — needs `bd close`. Bead still open. Per-specialist `template_sets` from config not loaded yet (only global `index.json` sets used). Filed as follow-up scope. |
| unitAI-an0o0 | P1 | Feature: mandatory rule templates | Parent epic of cdodj. Close after cdodj closes. |
| unitAI-1orcx | P1 | Re-review: runner injection after PARTIAL fixes | Reviewer was blocked on empty diff. Fixes are verified manually. Can dispatch fresh reviewer against master diff or just close as done-via-manual-verify. |
| unitAI-h76p2 | P1 | Review: tq1f API error surfacing fix | `tq1f` already merged (`165c8bf5`). Close both `h76p2` and `tq1f` — work is in master. |
| unitAI-tq1f | P1 | sp feed/result: surface API errors on zero-token runs | Merged `165c8bf5`. Close. |

### Backlog

| ID | P | Title | Context / Suggestions |
|----|---|-------|----------------------|
| unitAI-5y76a | P0 | Epic: 4.0 release | Parked. Activate after mandatory-rules chain fully closed. Needs: sync-docs pass, CHANGELOG, npm publish, GitHub release. |
| unitAI-nrdqb | P1 | Reviewer auto-load git diff | Partially mitigated by worktree checkpoint commits. Real fix: inject `git diff` context at reviewer startup. Key file: `src/specialist/runner.ts` reviewer diff injection (already has `buildReviewerDiffContext`). |
| unitAI-gm849 | P1 | Reviewer ignores staged-only diff | Related to nrdqb. Reviewer fails when all changes are staged not uncommitted. |
| unitAI-v3xb3 | P1 | Explorer output not appended to bead notes | READ_ONLY auto-append should fire but isn't. Check `supervisor.ts` auto-append path for waiting status. |
| unitAI-ssrx | P2 | Always-worktree design | Good design discussion item. Executors currently require explicit `--worktree` flag; proposal is to make it default for all edit-capable specialists. |
| unitAI-0vb3 | P2 | Architecture snapshot injection | Would let specialists understand the codebase structure without exploration turns. Needs design pass before impl. |
| unitAI-231x | P2 | Default context-depth to 3 | Low-risk change to `run.ts` default. Unblocks lazy chaining without explicit `--context-depth 2` every dispatch. |

## Memories Saved

No `bd remember` calls made this session. Key learnings for future sessions:

- After any `npm run build`, always run `npm install -g .` — stale global binary is silent and hard to detect
- mandatory-rules template files store rules as **body text** (not frontmatter `rules:` array) — parser has body-text fallback
- Token cap in runner must guard only the rules block size, not total prompt — bead task prompts are large by design

## Suggested Next Priority

1. **Close stale beads** (`unitAI-cdodj`, `unitAI-an0o0`, `unitAI-tq1f`, `unitAI-h76p2`) — all work is in master, beads are open. Keeps the board clean for the 4.0 release pass.

2. **Per-specialist template_sets** — `buildMandatoryRulesBlock` only loads global `index.json` sets; specialist configs declare `mandatory_rules.template_sets` but those are ignored. Small scoped fix in `mandatory-rules.ts`: accept specialist config as input and merge with global index.

3. **Start 4.0 release pass** (`unitAI-5y76a`) — sync-docs across all changed surfaces, CHANGELOG [Unreleased] population, GitHub release with commit-linked accomplishments, npm publish.

4. **Fix reviewer startup** (`unitAI-nrdqb`) — reviewer drift is the most painful operational blocker. The diff injection groundwork already exists in `runner.ts` (`buildReviewerDiffContext`); needs to be activated unconditionally for reviewer runs.
