---
session_date: 2026-06-15
branch: master
commits: 30
issues_closed: 5
issues_filed: 2
specialist_dispatches: 23
models_used: [openai-codex/gpt-5.5, openai-codex/gpt-5.4, openai-codex/gpt-5.4-mini]
handoff_bead: unitAI-hkide
---

# Session Report — 2026-06-15

## Summary

Closed the KAN-91 / `unitAI-gp7nq` epic ("expand global user config: broaden allowlist + model chains + preset references") by shipping Phases 2–4 plus the auto_commit decision and a follow-up canonical-docs chore. Five merges landed on master (`9d110adf` Phase 2, `e1a494ac` Phase 3, `994f6285` Phase 4, `953e2fea` chore canonical guide; Phase 0/1 were merged in earlier same-day compacts). The new runtime surface: `fallback_models: string[]` chains with in-flight transient-walk + `fallback_step` telemetry (Phase 2); `@preset/<name>` references resolved at spec-load time with four error classes (NotFound/Cycle/Config/Type), depth cap 4, cycle detection, and `preset_resolved` telemetry (Phase 3); `sp init --global` console hints surfacing every new knob category, a `_doc` sentinel filtered at the schema-preprocess layer in `GlobalUserConfigSchema`, and a 269-line canonical `docs/overrides-guide.md` written as a sidecar to `~/.config/specialists/overrides-guide.md` on every `sp init --global` (Phase 4 + `unitAI-hyugm` chore). Decision bead `unitAI-gp7nq.6` closed B — `execution.auto_commit` stays in `BLOCKED_OVERRIDE_FIELDS` because it is a recovery mechanism, not a preference (silent-loss failure mode too expensive for opt-out toggle; CI use case satisfied by fork-via `.specialists/user/<name>`). Two real security findings surfaced and were fixed in dedicated fix-loops — Phase 2 script-runner auth-error retry leak (`9772f2c2`) and Phase 3 preset-resolver type-validation gap PRESET-001 (`284883ec`); neither was caught by unit tests, seconder, or reviewer — only by `security-auditor`. The session also burned three procedural lessons worth keeping: (1) `sp clean --processes` is destructive on a crashed-job worktree with uncommitted work — it pruned the first Phase 3 executor's implementation completely and forced a full re-dispatch; (2) the bd auto-close hook fires on **any** seconder-completion event, including FAILED ones, so beads kept getting prematurely closed and had to be reopened; (3) the reviewer sandbox strips dots from bead ids (`unitAI-gp7nq.4` → `unitAI-gp7nq4`) so `bd show` from inside the reviewer fails — smoke evidence must be passed inline via `sp resume`. A parallel orchestrator in another tmux stream handled mercury work (`mmma.5` / `ub65d.4` worktree) — not in scope for this report.

## Issues Closed (5)

### KAN-91 epic — implementation phases

| ID | Title | Merge | Notes |
|----|-------|-------|-------|
| `unitAI-gp7nq.3` | Phase 2 — fallback model chains (string \| string[]) | `9d110adf` | 4 commits: feat + 2 fix-loops (telemetry semantics, in-flight transient walk) + script-runner auth-gate fix. Security-auditor caught FAL-001 (cross-provider prompt leak via `isRetryableModelFailure` not excluding auth) only after seconder/reviewer passed. |
| `unitAI-gp7nq.4` | Phase 3 — `@preset/<name>` reference resolution + cycle detection | `e1a494ac` | 3 commits: feat + fix-loop 1 (`SpecialistPresetConfigError` + `SpecialistPresetFieldMissingError`) + fix-loop 2 (`SpecialistPresetTypeError` post-resolve guard from security PRESET-001). NEW `src/specialist/preset-resolver.ts` with depth cap 4. `preset_resolved` telemetry event. |
| `unitAI-gp7nq.5` | Phase 4 — `sp init --global` console hints + `_doc` sentinel + upgrade-notes | `994f6285` | 3 commits: base + 2 doc fix-loops (delta-framing language; complete schema-valid example block bound via markdown→`validateGlobalUserConfig` test). `_doc` skip implemented at `GlobalUserConfigSchema` z.preprocess — upstream of loader iteration, so no `loader.ts` change needed. |

### KAN-91 epic — decision

| ID | Title | Resolution |
|----|-------|------------|
| `unitAI-gp7nq.6` | Decision: should `execution.auto_commit` be user-overridable? | **Option B**: keep blocked, fork-only. Rationale: `auto_commit` is a recovery mechanism not a user preference; the silent-loss failure mode (user sets `never` globally, next stall = lost turns) is too expensive for an opt-out toggle; the CI use case is properly satisfied by fork-via `.specialists/user/<name>`. Phase 3's `sp clean --processes` work-loss reinforced the bias. Follow-up chore `unitAI-hyugm` filed via `discovered-from`. |

### Follow-up chore

| ID | Title | Merge | Notes |
|----|-------|-------|-------|
| `unitAI-hyugm` | chore(specialist): canonical `docs/overrides-guide.md` + `sp init --global` sidecar emit | `953e2fea` | NEW canonical guide (269 lines, always-current reference) at root `docs/`, with explicit "`auto_commit` is fork-only by design" section closing `.6` in user-facing docs. Sidecar emitted to `<global-config-dir>/overrides-guide.md` on every `sp init --global`. `GLOBAL_USER_CONFIG_DOC` repointed to `./overrides-guide.md`. Upgrade-note repurposed as historical KAN-91 delta record. Binding test moved from upgrade-note to canonical guide. |

## Issues Filed

| ID | P | Type | Title | Why |
|----|---|------|-------|-----|
| `unitAI-hyugm` | P3 | chore | canonical `docs/overrides-guide.md` + `sp init --global` sidecar emit | Discovered while answering operator question "include `## commented guide` directly in user.json?". JSON has no comments; JSONC/JSON5 swap is too expensive for the UX win. Sidecar copy of canonical guide is the right shape — user opens user.json + guide side-by-side in editor, single source of truth, no parser change. Linked `discovered-from:unitAI-gp7nq.6` so decision context survives. |
| `unitAI-hkide` | P1 | task | Session handoff 2026-06-15: KAN-91 epic shipped — pick up next priorities | Mandatory handoff bead per session-close-report skill Step 8. Anchors this SSOT report and links to suggested next-priority beads. |

## Specialist Dispatches

23 dispatches over four chains (Phase 2 chain ran in earlier compact). All chains used canonical pipeline (writer → seconder → test-engineer/test-runner → security-auditor when relevant → obligations-scanner → reviewer). `--worktree` for first executor, `--job <exec-job>` for everyone downstream.

### Chain summary

| Chain root | Bead | Phases of work | Final verdict |
|-----------|------|----------------|---------------|
| (prior session) | `unitAI-gp7nq.3` | executor + 3 fix-loops + obligations + reviewer | PASS @ 132/132 tests |
| `a03aef` | `unitAI-gp7nq.4` | executor (3 crashes) + 2 fix-loops + seconder ×3 + security ×2 + reviewer (PARTIAL→PASS via inline smoke) | PASS @ 100/100 targeted tests |
| `b4cb63` | `unitAI-gp7nq.5` | executor + 2 doc fix-loops + seconder + obligations + reviewer (PARTIAL ×2 → PASS) | PASS |
| `ced45d` | `unitAI-hyugm` | executor + seconder + obligations + reviewer (clean first pass) | PASS |
| (operator-direct) | `unitAI-gp7nq.6` | overthinker analysis collapsed into orchestrator turn — operator-decided B | closed |

### Dispatch detail (this session window)

| Wave | Job | Specialist | Model | Bead | Outcome |
|------|-----|-----------|-------|------|---------|
| 1 | `cdd15b` | executor | gpt-5.5 | `unitAI-gp7nq.4` | **Crashed at 8m24s mid-vitest** — work pruned by my `sp clean --processes` (see Problems). |
| 1' | `a03aef` | executor | gpt-5.5 | `unitAI-gp7nq.4` | **Crashed at ~4m mid-vitest** — orchestrator verified + committed work as `77aa841c`. |
| 2 | `8d026b` | seconder | gpt-5.4 | `unitAI-gp7nq.4` | FAIL — LQSHA injected-diff bug (saw only `.xtrm/skills/` noise). Worktree noise discarded; re-dispatched. |
| 2' | `c8a0f0` | seconder | gpt-5.4 | `unitAI-gp7nq.4` | FAIL — real findings: `loadPresets` JSON parse silent fail + `resolvePresetReference` undefined-field silent return. |
| 3 | `f32e24` | executor (fix-loop 1) | gpt-5.5 | `unitAI-gp7nq.4` | PASS — added `SpecialistPresetConfigError` + `SpecialistPresetFieldMissingError`, committed `91a7a5d8`. |
| 4 | `21d0f6` | seconder | gpt-5.4 | `unitAI-gp7nq.4` | quality PASS / scope UNCLEAR (smoke evidence not in diff — procedural). |
| 5 | `fbe931` | test-runner | — | `unitAI-gp7nq.4` | **Crashed silently at 3m46s** — no error log. Retried. |
| 5' | `dca79a` | test-runner | — | `unitAI-gp7nq.4` | **Crashed again at ~90s** — fell back to direct `bun --bun vitest run` (orchestrator). |
| 6 | `460aaa` | obligations-scanner | gpt-5.4-mini | `unitAI-gp7nq.4` | CLEAN. |
| 7 | `36718e` | security-auditor | gpt-5.4 | `unitAI-gp7nq.4` | **PRESET-001 medium** — preset-resolver returns raw values without type validation; non-string preset payload can reach runner as model id. |
| 8 | `e47176` | executor (fix-loop 2) | gpt-5.5 | `unitAI-gp7nq.4` | **Killed by tmux crash** before substantive work landed. |
| 8' | `cdcad9` | executor (fix-loop 2) | gpt-5.5 | `unitAI-gp7nq.4` | PASS — added `SpecialistPresetTypeError`, committed `284883ec`. |
| 9 | `5bf70d` | security-auditor (re-pass) | gpt-5.4 | `unitAI-gp7nq.4` | **PASS** — 0 findings, `verdict: pass`. |
| 10 | `1e4dd2` | reviewer | gpt-5.4 | `unitAI-gp7nq.4` | PARTIAL — sandbox `bd show` strips dot from id; orchestrator resumed with inline smoke evidence → **PASS**. |
| 11 | `b4cb63` | executor | gpt-5.5 | `unitAI-gp7nq.5` | PASS — committed `eac428c2`. |
| 12 | `d67872` | seconder | gpt-5.4 | `unitAI-gp7nq.5` | quality PASS / scope PARTIAL (loader.ts not touched — covered by schema preprocess). |
| 13 | `aa5278` | executor (fix-loop 1) | gpt-5.5 | `unitAI-gp7nq.5` | PASS — framing language for delta-snippets, committed `b731fad7`. |
| 14 | `e25bf1` | obligations-scanner | gpt-5.4-mini | `unitAI-gp7nq.5` | CLEAN. |
| 15 | `b0ed1c` | reviewer | gpt-5.4 | `unitAI-gp7nq.5` | PARTIAL ×2 — wanted full schema-valid example in docs. |
| 16 | `ab422c` | executor (fix-loop 2) | gpt-5.5 | `unitAI-gp7nq.5` | PASS — added complete-example block + binding test, committed `607da584`. Reviewer re-pass → **PASS**. |
| 17 | `ced45d` | executor | gpt-5.5 | `unitAI-hyugm` | PASS — committed `e9d10599`. |
| 18 | `bbba0d` | seconder | gpt-5.4 | `unitAI-hyugm` | PASS clean. |
| 19 | `25c4ca` | obligations-scanner | gpt-5.4-mini | `unitAI-hyugm` | CLEAN. |
| 20 | `ee4e2b` | reviewer | gpt-5.4 | `unitAI-hyugm` | **PASS first pass** — only chain that hit PASS without fix-loops. |

## Problems Encountered

| Problem | Root Cause | Resolution |
|---------|-----------|------------|
| First Phase 3 executor (`cdd15b`) crashed at 8m24s mid-vitest; I ran `sp clean --processes` to mop stale Phase 2 jobs, which **pruned the executor's worktree** — `preset-resolver.ts`, tests, docs all destroyed. | `sp clean --processes` removes worktrees of CRASHED jobs even when work is uncommitted. The crashed job's worktree was treated as discardable garbage. | Re-dispatched executor entirely (`a03aef`), lost ~12 min. **Saved memory `sp-clean-processes-destroys-uncommitted-worktree-work-sp`** — rule: BEFORE running `sp clean --processes` after a crash, FIRST `cd` to the worktree, `git add . && git commit -m "WIP"`. Or use targeted `kill -9 <pid>` for specific stale PIDs. |
| Three consecutive executor crashes in Phase 3 (`cdd15b`/`a03aef`/`e47176`) all mid-vitest at ~4–8 min with `gpt-5.5`. | Suspected resource/IPC race during `bun --bun vitest run tests/unit/specialist/`. Pattern: full implementation lands in worktree, then vitest call → silent process death. Same pattern observed in Phase 2 memory. | Verified work externally (orchestrator ran `bun --bun vitest` directly), committed on executor's behalf, continued chain. Restrict executor test runs to specific touched files explicitly to avoid the full-suite hang. |
| `test-runner` specialist crashed silently twice on Phase 3 (`fbe931` 3m46s, `dca79a` ~90s). | Same crash family as the executor mid-vitest deaths — no error log, just `Process crashed or was killed`. | Fell back to orchestrator running `bun --bun vitest run` directly with `--exclude tests/unit/specialist/supervisor.test.ts` (the known FIFO-hang quarantine file); recorded full evidence in bead notes for reviewer to consume. |
| LQSHA injected-diff bug recurred on Phase 3 seconder iter 1 (`8d026b`) — seconder saw only `.xtrm/skills/active/*` + `AGENTS.md` unstaged noise as the "writer diff" instead of the actual committed implementation. | `runtime/src/cli/run.ts buildInjectedReviewerDiffVariables` `AUTO_COMMIT_NOISE_PREFIXES` filter is in place from a prior session, but the unstaged noise files still produced a noise-only diff that beat the branch-vs-base diff. | `git checkout -- AGENTS.md CLAUDE.md .xtrm/skills/active/` in the worktree, re-dispatched seconder. Worked. Documented pattern. |
| Phase 3 bead auto-closed prematurely after the first seconder (`8d026b`) **FAILED**. | The bd auto-close hook fires on any seconder run-completion, regardless of verdict. Close reason was "Specialist seconder completed (job 8d026b)" even on a FAIL verdict. | `bd update --status=in_progress` to reopen, continued. Same pattern recurred on `unitAI-hyugm` after executor `ced45d` completion. **The hook needs verdict-aware gating** — follow-up filed (see Open Issues). |
| Reviewer (`1e4dd2`) PARTIAL on Phase 3 citing "no smoke evidence" — but the orchestrator had recorded smoke in bead notes. | Reviewer's sandbox `bd show unitAI-gp7nq.4` strips the dot before the `4` (`unitAI-gp7nq4`), so the call fails and the reviewer never reads the bead notes. | Resumed reviewer via `sp resume` with smoke evidence inlined in the resume prompt. Reviewer re-evaluated → PASS. |
| Security-auditor PRESET-001 medium finding: preset-resolver returned raw `preset.fields[fieldPath]` values without type validation; a malformed preset payload (e.g. `{nested: "obj"}` under `model`) could reach runner/model-chain as a non-string. | Resolver focused on the reference syntax (name → lookup → recurse) and forgot the value-shape contract. Same blind spot as Phase 2's `isRetryableModelFailure` auth gap — unit tests, seconder, and reviewer all passed; only security-auditor caught it. | Fix-loop 2 added `SpecialistPresetTypeError` with per-field-path typeof + array-element guards inside `resolvePresetReference`. Re-pass clean. |
| Tmux session crash mid-Phase 3 — killed `e47176` executor that was in mid-fix-loop. | Out of scope (host tmux). | Re-dispatched as `cdcad9` from `bd show` context; the bead notes carried the fix-loop instructions, so no context lost. |
| Reviewer (`b0ed1c`) gave PARTIAL twice on Phase 4 because doc snippets are partial deltas, not standalone schema-valid `user.json` files. | Bead VALIDATION criterion read literally: "examples are valid JSON that `validateGlobalUserConfig` accepts." Deltas don't satisfy that literally even if they're obviously fragments. | Fix-loop 2 added a "Complete example (validates against `GlobalUserConfigSchema`)" section + binding test that extracts the JSON block from the markdown and runs it through `validateGlobalUserConfig`. Doc now bound to schema permanently. |
| Cleanup at session close revealed 10+ orphan `serena` MCP processes pointing at deleted worktrees (`.worktrees/unitAI-gp7nq.4/...`, `.worktrees/unitAI-hyugm/...`). RSS dropped from 5944 MB → 2088 MB after killing them. | `git worktree remove` does not propagate to subprocess MCP servers that were spawned with the worktree as `--project` arg; those processes outlive the worktree directory. | Killed manually at session close (Step 0). Reinforces `unitAI-c4g0m` (LSP/Serena gateway epic) as load-bearing — every dispatched executor spins up a fresh serena, and the leak compounds across a long session. |

## Code Changes

### NEW runtime files (KAN-91)
- `src/specialist/preset-resolver.ts` — 118 lines after fix-loops. Pure resolver with `loadPresets()` cache, `resolvePresetReference()`, depth cap 4, cycle detection via visited `Set`, four error classes (NotFound/Cycle/Config/Type) all carrying spec name + field path + preset name + known presets in their messages. `preset_resolved` debug event emitted per resolution.
- `src/specialist/model-chain.ts` — Phase 2 helper (`resolveModelChain` with plural-wins precedence, dedupe primary, `console.debug` when plural+singular both set).

### Touched runtime files
- `src/specialist/loader.ts` — `applyOverrideFields` routes `@preset/`-prefixed values through `resolvePresetReference` before writing the merged spec. Resolves in both user-override layer AND package canonical layer.
- `src/specialist/schema.ts` — `fallback_models: z.array(z.string()).nullable().optional()` added; `'fallback_models'` added to `OVERRIDE_ALLOWED_EXECUTION_FIELDS`.
- `src/specialist/runner.ts` — Phase 2 outer chain-walk loop at line ~1384 wraps the existing transient-retry loop; emits `fallback_step` event on advance with `{specialist, attempt_n, model_tried, error_class, terminal}`.
- `src/specialist/script-runner.ts` — Phase 2 auth-gate fix (`isAuthFailureMessage`, gate `parsed.retryable && parsed.errorType !== 'auth'` in `classifyAttempt`). Closes FAL-001 cross-provider prompt leak risk.
- `src/specialist/global-config.ts` — `GlobalUserConfigSchema` rewrapped as `z.preprocess(filter !key.startsWith('_'), z.record(...))`. `GLOBAL_USER_CONFIG_DOC` constant exported (initially package-path, then repointed to `./overrides-guide.md` in the chore). `buildGlobalUserConfigTemplate` seeds `_doc` by default.
- `src/cli/init.ts` — `sp init --global` post-create console block prints four dim/yellow hint lines (extension opt-out, `system_prompt_mode`, `fallback_models`, `@preset/cheap`). Chore added sidecar emit: locate `docs/overrides-guide.md` via `resolveCanonicalAssetDir`, copy to `<global-config-dir>/overrides-guide.md`, print one extra hint line.
- `src/cli/edit.ts` — `loadPresets` lifted out into `preset-resolver.ts` as the single source of truth; edit.ts imports from there.

### NEW docs
- `docs/overrides-guide.md` (269 lines) — **canonical, always-current** reference for the global `user.json` override surface. Lifted per-field reference + preset syntax + fallback semantics from the upgrade-note and rewritten as "current shape" not "delta". Explicit "`auto_commit` is fork-only by design" section closes `unitAI-gp7nq.6` in user docs.
- `docs/upgrade-notes/kan-91-expanded-overrides.md` (205 lines after fix-loops) — historical KAN-91 delta record. Now cross-references the canonical guide at the top.

### Tests
- `tests/unit/specialist/preset-resolver.test.ts` — 89 lines, 5 cases (NotFound, Cycle, Depth-cap, non-preset-passthrough, dedupe).
- `tests/unit/specialist/model-chain.test.ts` — Phase 2 precedence cases.
- `tests/unit/specialist/loader.test.ts` — regression for preset-resolution in user-override + package layers; 54/54 pass.
- `tests/unit/specialist/global-config.test.ts` — `_doc` validation/merge tests + markdown→`validateGlobalUserConfig` binding test (extracts the JSON block from `docs/overrides-guide.md` and asserts valid=true).
- `tests/unit/specialist/runner.test.ts` — chain-walk tests (transient-advance, all-fail, auth-no-walk).
- `tests/unit/specialist/script-runner.test.ts` — auth-no-walk tests.

### Other
- `CHANGELOG.md` `[Unreleased]` — Phase 4 executor appended five KAN-91 bullets across Phases 0–4. Verified at session close; no further updates needed.
- `package.json` — `files` allowlist already globs `docs/`; chore confirmed sidecar source ships in the npm package.

## Documentation Updates

- **NEW** `docs/overrides-guide.md` — canonical reference (above).
- **Updated** `docs/upgrade-notes/kan-91-expanded-overrides.md` — added cross-ref pointing readers at the canonical guide; added "snippets are deltas" framing language in the Overview; appended schema-valid Complete Example block in the doc-fix-loop 2.
- **CHANGELOG.md** `[Unreleased]` — five KAN-91 bullets added (Phase 4 executor did this; orchestrator verified at session close).
- **`bd remember`** saved 6 memories this session (see Memories Saved below).
- **Service skills**: no service registry under `.claude/skills/service-registry.json` — N/A.
- **CLAUDE.md**: no top-level workflow or service change — left untouched.
- **Sync-docs**: no architecture / migration / public API change in this session — skipped.

## Open Issues with Context

### KAN-91 follow-ups (ready for next session)

| ID | P | Title | Context / Suggestions |
|----|---|-------|----------------------|
| `unitAI-gp7nq.8` | P2 | Test: Phase 1 — six allowlisted user-environment fields (per-field merge + live `sp config show`) | Companion test bead. Phase 1 (`unitAI-gp7nq.2`) merged at `75a8a1c3` earlier today. Live `sp config show` is currently blocked when the worktree lacks `.specialists/catalog/index.json` — note this in the test plan; the binding test pattern from Phase 4 (`tests/unit/specialist/global-config.test.ts:markdown-binding`) is the right shape. |
| `unitAI-gp7nq.9` | P2 | Test: Phase 2 — fallback chains schema + runtime walk + `fallback_step` telemetry | Phase 2 merged at `9d110adf`. Synthetic smoke `/tmp/phase2-smoke.mjs` (operator-authored) covers precedence cases; the bead wants a runtime walk + telemetry assertion. Note: `gpt-5.5` executor crashed mid-vitest 3× this session — author tests in a separate file from `supervisor.test.ts` (FIFO-hang quarantine) and run them in isolation. |
| `unitAI-gp7nq.10` | P3 | Test: Phase 3 — preset reference resolver (math + loader hook + `preset_resolved` telemetry) | Phase 3 merged at `e1a494ac`. Most of the math is already covered in `tests/unit/specialist/preset-resolver.test.ts` (5 cases, all pass). Gap: telemetry assertion + loader-hook integration test for package-canonical-layer resolution. |
| `unitAI-gp7nq.11` | P2 | Test: Phase 4 — `sp init --global` stub + docs manual smoke checklist | Phase 4 merged at `994f6285`. The orchestrator-run manual smoke (clean-HOME + pre-existing user.json + `_doc` survival + loader tolerance) is documented in bead notes — turn the recipe into an automated test. The binding test from `tests/unit/specialist/global-config.test.ts` is the canonical example. |
| `unitAI-aav4w` | P2 | Backfill KAN-90 in canonical reference docs (README, cli-reference, features, ARCHITECTURE) | Sibling to Phase 4. Out of scope for the gp7nq epic per the Phase 4 NON_GOALS. Now that the canonical `docs/overrides-guide.md` exists, this bead can cross-reference it instead of duplicating content. |
| `unitAI-v4i0j` | P1 | Fix stale 4-layer docs in CHANGELOG + kan-90 upgrade notes | Phase 4 NON_GOALS deferred this. Worth a small cleanup pass — likely <30 lines of doc diff. |
| `unitAI-ucpcy` | P1 | `sp init --global` should not bake repo-local `.specialists/user/` specs into the global file | Discovered earlier (not this session); the canonical guide chore (`unitAI-hyugm`) introduced the sidecar pattern which makes this easier to fix — emit the sidecar based on `package-canonical` specialists only, never the repo-local user-tier set. |

### Procedural follow-ups discovered this session (need files)

- **bd auto-close hook verdict-gating** — Every chain in this session hit the premature-close pattern (seconder FAIL still closed the bead). The hook needs verdict-aware gating: only auto-close on seconder `overall_verdict=PASS` + reviewer `PASS`, never on FAIL/PARTIAL.
- **Reviewer sandbox dot-drop in bead-id resolution** — `bd show unitAI-gp7nq.4` from inside the reviewer specialist strips the dot, fails the call, and the reviewer cannot read bead notes. Forces orchestrator to pass smoke evidence inline via `sp resume`. Worth investigating the dotted-id round-trip across pi/MCP/bash boundary.

These were not filed as separate beads this session — operator may want to triage and file when convenient.

### Other (backlog — untouched this session)

The skeleton's full open-issues table covers 50+ unrelated backlog beads — see `bd list --status=open --priority=0,1` for the actionable subset. Highlights to flag for next session:

- `unitAI-zadhq` P0 — HANDOFF 2026-06-06: telemetry consolidation pickup roadmap (carry-over).
- `unitAI-c4g0m` P0 — LSP/Serena gateway epic; this session **made it more relevant** by leaking 10+ serena MCP processes (cleaned up at session close — see Step 0 in skill workflow).
- `unitAI-kj651` P1 — Specialists runtime cleanup + substrate alignment HEAD epic.
- `unitAI-dnw8v` P1 — esbuild CVE bump (GHSA-gv7w-rqvm-qjhr).

## Memories Saved

| Key | Content |
|-----|---------|
| `phase-2-unitai-gp7nq-3-shipped-2026-06` | Phase 2 closure record (from earlier same-day session before this compact). |
| `phase-3-unitai-gp7nq-4-shipped-2026-06` | Phase 3 closure: merge `e1a494ac`, three commits, four error classes, depth cap 4, smoke happy+sad PASS via direct loader probe, security re-pass PASS. Procedural lessons (sp clean destroying worktrees, auto-close hook, dot-drop). |
| `phase-4-unitai-gp7nq-5-shipped-2026-06` | Phase 4 closure: merge `994f6285`, three commits, `_doc` at schema-preprocess layer (no loader.ts change), 269-line upgrade-notes + complete-example binding test. Console hints landed in `init.ts:881+`. |
| `decision-auto-commit-fork-only-2026-06-15` | `unitAI-gp7nq.6` closed B (keep blocked). Rationale: recovery mechanism not preference; silent-loss too expensive; CI use case satisfied by fork. Phase 3 work-loss reinforced. |
| `unitai-hyugm-canonical-overrides-guide-shipped-2026-06-15` | Canonical `docs/overrides-guide.md` is the SSOT for the user.json override surface; upgrade-notes is now historical. Sidecar emit pattern documented. |
| `sp-clean-processes-destroys-uncommitted-worktree-work-sp` | Rule: NEVER `sp clean --processes` after an executor crash without committing the worktree first. Use targeted `kill -9 <pid>` instead. |

## Suggested Next Priority

1. **`unitAI-v4i0j`** (P1, ~30 lines) — Fix stale 4-layer docs in CHANGELOG + kan-90 upgrade-notes. Tiny doc-only cleanup, cheap win, removes a known stale page reader will hit before they find the canonical guide.
2. **`unitAI-gp7nq.8` / `.9` / `.10` / `.11`** (P2/P3, companion test beads) — Now that Phases 1–4 are merged, the test beads can land in a single small session. Reuse the markdown→`validateGlobalUserConfig` binding test pattern from Phase 4 for `.11`; reuse `/tmp/phase2-smoke.mjs` patterns for `.9`.
3. **File the two procedural follow-ups** (bd auto-close verdict-gating + reviewer dot-drop) as P3 bug beads. Both are small, both bit us repeatedly this session.
4. **`unitAI-c4g0m`** (P0, LSP/Serena gateway epic) — This session leaked 10+ orphan serena MCP processes after worktree removal (RSS 5944 → 2088 MB after cleanup). The leak pattern reinforces the case for the shared LSP gateway epic — every dispatched executor spins up a fresh serena, and worktree-prune doesn't kill them.
