---
session_date: 2026-04-28
branch: master
commits: 7
issues_closed: 11
issues_filed: 7
specialist_dispatches: 0
models_used: []
---

# Session Report — 2026-04-28

## Summary

Substantial multi-phase session: opened with a runtime bug investigation that exposed three related defects in the supervisor's initial-turn-to-waiting persistence path (no `upsertResult` call, deferred bead-notes append, unrecoverable stopped-job output) and shipped a single fix mirroring `handleResumeTurn`. Pivoted to backlog cleanup — closed `lcv6` as obsolete after fln4q-epic, fixed test-isolation contamination in `qsdt6` (XDG_DATA_HOME leak across describe blocks), patched 8 specialist configs in `c6he0` to teach explorer/debugger/executor/reviewer about gitnexus CLI fallback when MCP tools aren't loaded. Discovered during the rczcp Serena-adoption soak that a stale `.specialists/user/explorer.specialist.json` overlay was silently shadowing the default's mandatory_rules — filed `bb3h6` for detection, then implemented v1 in `sp doctor`. Cleaned 39 leftover worktrees, ~298 orphan dirs, and 260+ stale branches. Then took on three substantial sp-serve features in autonomous mode: `/readyz` endpoint with 6-reason taxonomy and audit-failure counter (daxxp), trust flags with default-reject + sha256 skill_sources audit (3k6sa), and hot-reload watcher v1 with native fs.watch + `--reload-poll-ms` polling fallback (or6kw). Authored a new `using-script-specialists` skill for the `sp script` / `sp serve` paradigm, updated `using-specialists-v2` and `CLAUDE.md` for 3.9/3.10 drift (context-depth default 3, auto-worktree, stale-base guard, auto-checkpoint, per-turn bead-notes append, sp poll deprecation, list-rules CLI). All commits pushed to origin/master with typecheck + build clean and 35 new tests, all green. No specialist dispatches — every task was small enough to be in-scope for direct implementation per CLAUDE.md guidance.

## Issues Closed (11)

### Bug fixes

| ID | Title | Close reason |
|----|-------|--------------|
| unitAI-8b812 | Fix: sp result + bead notes empty on waiting specialists | Patched supervisor.ts initial-turn path with `upsertResult` + inline `appendResultToInputBead` + `skipFinalKeepAliveInputBeadAppend`; result.ts gains `run_complete` event-payload fallback. Verified live: sp result returns output on waiting and after sp stop; bead notes append immediately. |
| unitAI-c6he0 | Specialists with gitnexus-required template should use gitnexus CLI when MCP tools unavailable | Patched explorer/debugger/executor/reviewer specs (config + .specialists/default mirrored): system+task_template now show MCP↔CLI equivalents inline. Reviewer accepts either MCP `gitnexus_impact` calls or `npx gitnexus impact` CLI invocations as evidence. |
| unitAI-qsdt6 | bug: observability-sqlite node tests fail when run with observability-db + cli/db | Added save+restore of `process.env.XDG_DATA_HOME` to observability-db.test.ts beforeEach/afterEach (mirrors db.test.ts pattern). Trio passes 48/48 in all orderings. |
| unitAI-zleim | Observability: mandatory_rules_injection meta event misses spec-resolved sets | Closed not-a-bug: meta event was accurate. Root cause was stale `.specialists/user/explorer.specialist.json` shadowing the default. Filed bb3h6 for drift detection. |

### Backlog audit / closure

| ID | Title | Close reason |
|----|-------|--------------|
| unitAI-lcv6 | Test: 08zd Phase 3B — supervisor dual-write verification | Obsolete after fln4q-epic (file output env-gated, DB-first reads). Existing observability-sqlite.test.ts (56 tests) + supervisor.test.ts + per-CLI tests cover the persistence + read paths. |
| unitAI-rczcp | Re-check Serena adoption rate after serena-cheatsheet rule | Deferred — soak target was 2026-04-29 with ≥30 diverse-specialist jobs; only 2 explorer jobs ran post-rule. Re-soak window: 2026-05-05. |

### Features

| ID | Title | Close reason |
|----|-------|--------------|
| unitAI-bb3h6 | Detect/warn stale `.specialists/user/` overlays | v1 shipped: `sp doctor` "User specialist overlays" section warns when overlay shadows default with empty mandatory_rules.template_sets. Verified live (caught the stale explorer overlay). |
| unitAI-daxxp | sp serve /readyz endpoint with degraded states | 6-reason taxonomy (draining / degraded:audit / pi_config_unreadable / db_not_writable / empty_user_dir / invalid_spec_in_user_dir) + sliding-window audit-failure counter + SIGTERM draining. /healthz unchanged. |
| unitAI-3k6sa | sp serve trust flags | `--allow-skills` / `--allow-skills-roots` / `--allow-local-scripts` with default-reject compatGuard, sha256 skill_sources audit when allowSkills enabled. |
| unitAI-or6kw | sp serve hot-reload of specialist files | v1: native fs.watch (default) + `--reload-poll-ms` polling fallback, 300ms debounce, loader.invalidateCache(name) per change, clean SIGTERM teardown. |

### Cleanup

| ID | Title | Close reason |
|----|-------|--------------|
| unitAI-f9v4w | Cleanup: stale user specialist overlay + leftover worktrees | Deleted `.specialists/user/explorer.specialist.json`, committed `docs/design/shepherd.md`, paved 39 worktrees + ~298 orphan dirs + 260+ stale branches. |
| unitAI-x2msh | Chore: post-reindex AGENTS/CLAUDE stat sync | Routine gitnexus reindex stat updates committed after worktree+branch sweep. |

## Issues Filed (7)

| ID | P | Type | Title | Why |
|----|---|------|-------|-----|
| unitAI-0zz7a | P3 | task | Test: sp result on waiting job | Repro bead for the supervisor result/bead-notes bug investigation. Auto-closed when test job stopped. |
| unitAI-vo7wr | P3 | task | Test: re-verify sp result fix on waiting | Verification bead used to confirm the 8b812 fix landed correctly on a fresh keep-alive explorer run. Auto-closed via `sp stop`. |
| unitAI-8b812 | P1 | bug | Fix: sp result + bead notes empty on waiting specialists | Filed as the formal bead for the three-defect fix discovered during the repro. Closed same session. |
| unitAI-zleim | P2 | bug | Observability: mandatory_rules_injection meta event misses spec-resolved sets | Filed during rczcp investigation when meta event appeared to be missing spec template_sets. Closed not-a-bug after discovering the user-overlay shadow. |
| unitAI-bb3h6 | P2 | feature | Detect/warn stale `.specialists/user/` overlays vs default+config drift | Discovered-from zleim. Stale overlays silently bypass new mandatory rules and prompt fixes (e.g. c6he0). Implemented v1 same session. |
| unitAI-f9v4w | P3 | chore | Cleanup: stale user specialist overlay + leftover worktrees | Filed to bracket the user-authorized cleanup phase under a tracked claim. |
| unitAI-x2msh | P4 | chore | Post-reindex AGENTS/CLAUDE stat sync after worktree+branch sweep | Filed to bracket the routine post-reindex commit under a tracked claim. |

## Problems Encountered

| Problem | Root Cause | Resolution |
|---------|-----------|------------|
| `sp result` on waiting specialist returned only the "waiting for input" indicator with no output | supervisor.ts:1814–1832 (initial-turn-to-waiting path) emitted `run_complete` with output embedded as a timeline event but never called `client.upsertResult(id, output)`. With file output env-gated off (default in 3.9+), neither `specialist_results` nor `result.txt` had the data. | Mirrored `handleResumeTurn` pattern: added `upsertResult` after `emitRunCompleteForTurn`. |
| Bead notes stayed empty on waiting checkpoints despite per-turn auto-append being documented as universal | `appendResultToInputBead` was only called at line 1848 *after* `keepAliveExitPromise` resolved. First-turn waiting state never reached that line until session terminally exited. | Inlined `appendResultToInputBead({status:'waiting'})` before `setWaitingStatus()` and set `skipFinalKeepAliveInputBeadAppend = true` to avoid double-write at exit. |
| `sp stop` then `sp result` returned "Result not found" even though output was sitting in the `run_complete` event payload | `result.ts:readResultOutput` only consulted `specialist_results` and `result.txt` — neither populated for stopped-before-final-turn jobs. | Added defensive fallback: scan timeline events in reverse for the latest `run_complete` with non-empty output. |
| Explorer specialists fall through to grep/Read instead of using GitNexus, reviewer flags missing evidence | Specialist system+task_template prompts hard-instruct MCP-style calls (`gitnexus_query({...})`) without showing the CLI equivalent. The mandatory rule already had both forms, but rule guidance is overridden by spec prompt content. | Patched 4 specs (config + .specialists/default mirrored): inline `MCP form ↔ npx gitnexus CLI form` equivalents; reviewer accepts either evidence. |
| `bv --robot-triage` output for rczcp showed Serena=0 calls — soak appeared to have failed | Two compounding issues: (1) sample size too small (only 2 explorer jobs ran post-rule, both my own test repros); (2) the `mandatory_rules_injection` meta event for explorer 296b39 listed only global default sets — no `serena-cheatsheet`. Investigation revealed `.specialists/user/explorer.specialist.json` was shadowing the default with empty mandatory_rules. | Deferred rczcp re-soak to 2026-05-05; deleted the stale user overlay; implemented `sp doctor` user-overlay drift detection. |
| Test trio (observability-sqlite + observability-db + cli/db) failed when run together with `bootstrapNode: fail node_started` after 5 retries | observability-db.test.ts at line 107 set `process.env.XDG_DATA_HOME = xdgHome` without restoring. `resolveDbDirectory` reads XDG_DATA_HOME first and ignores cwd/gitRoot — leaked value redirected subsequent `createObservabilitySqliteClient(tempRoot)` calls to the deleted tempdir, opening a fresh schemaless DB. | Captured ORIGINAL_XDG at describe scope; restored in afterEach. Mirrors db.test.ts canonical pattern. |
| dolt remote auto-push failed on every `bd` write | RPC permission denied on `https://doltremoteapi.dolthub.com/jaggerxtrm/specialists`. | Pre-existing infrastructure issue, not session-introduced. Local writes succeed; treated as warning noise. |

## Code Changes

### Bug fix — supervisor result/bead persistence (`8b812`)

- `src/specialist/supervisor.ts` — added `upsertResult` and inline `appendResultToInputBead` on the initial-turn-to-waiting path (~line 1814). Mirrors `handleResumeTurn:1259-1298`. Sets `skipFinalKeepAliveInputBeadAppend = true` to prevent double-write at session exit.
- `src/cli/result.ts` — defensive fallback in `readResultOutput`: when both `specialist_results` and `result.txt` are empty, scan timeline events in reverse for the latest `run_complete` with non-empty output. Recovers stopped-job output and any pre-fix orphans.

### Specialist prompt drift (`c6he0`)

- 8 files updated (config/specialists/ + .specialists/default/ mirrored): explorer, debugger, executor, reviewer. System prompts and task_templates now show MCP↔CLI equivalents inline (`gitnexus_query({query})` / `npx gitnexus query "<text>"` etc). Reviewer task_template explicitly accepts either evidence form.

### Test isolation (`qsdt6`)

- `tests/unit/specialist/observability-db.test.ts` — save+restore `process.env.XDG_DATA_HOME` in describe-scope ORIGINAL_XDG + afterEach.

### sp serve features

- `src/cli/serve.ts` — major restructure across the three features:
  - `daxxp`: ReadinessState, recordAuditFailure, evaluateReadiness (exported, pure async fn), `/readyz` handler, SIGTERM flips draining, `--audit-failure-threshold` flag.
  - `3k6sa`: `--allow-skills` / `--allow-skills-roots <p1>:<p2>` / `--allow-local-scripts` flags threaded into `ScriptRunnerOptions.trust`.
  - `or6kw`: createUserDirWatcher wiring + clean SIGTERM teardown + `--reload-poll-ms` flag.
- `src/cli/serve-hot-reload.ts` (new) — `createUserDirWatcher` with native fs.watch / mtime-polling dual mode, debounced loader.invalidateCache.
- `src/specialist/script-runner.ts` — TrustOptions interface, compatGuard accepts trust + enforces allowSkillsRoots prefix check, computeSkillSources(spec) returning `{path, sha256}[]`, writeTraceRow embeds skill_sources into status_json, onAuditFailure callback in ScriptRunnerOptions wraps upsertStatus/upsertResult in try/catch.

### Drift detection (`bb3h6`)

- `src/cli/doctor.ts` — added `USER_SPECIALISTS_DIR` const, `checkUserOverlayDrift` function, wired into `run()`. Compares each user overlay's mandatory_rules.template_sets against the same-named default spec; warns on missing entries.

### Skill + docs drift sync

- `config/skills/using-script-specialists/SKILL.md` (new) — covers `sp script` / `sp serve` synchronous READ_ONLY runtime; compatGuard rules, template variables, HTTP contract.
- `config/skills/using-specialists-v2/SKILL.md` — bumped to v1.1 with 3.9/3.10 drift fixes.
- `CLAUDE.md` — updated specialists table, command surface, invariants section, runtime architecture, key files.

### Cleanup (`f9v4w`)

- Deleted `.specialists/user/explorer.specialist.json` (stale overlay shadowing default).
- Committed `docs/design/shepherd.md` (pre-existing untracked design draft).
- Removed 39 worktrees, ~298 orphan `.worktrees/` dirs, 260+ stale branches.

## Documentation Updates

| Type | Item |
|------|------|
| New skill | `config/skills/using-script-specialists/SKILL.md` (208 lines) |
| Updated skill | `config/skills/using-specialists-v2/SKILL.md` v1.1 — context-depth default 3, auto-worktree, --no-worktree removed, --force-stale-base, auto-checkpoint, per-turn bead append for all specialists, sp poll deprecation, sp list-rules, sp result on waiting jobs |
| Updated docs | `CLAUDE.md` — same drift fixes + added pointer to using-script-specialists; key files list expanded with `script.ts`/`serve.ts`/`script-runner.ts`/`list-rules.ts`/`result.ts`; runtime architecture section now lists script-class CLI surface, /lib export, observability DB sharing |
| New design draft | `docs/design/shepherd.md` (pre-existing untracked, committed under f9v4w) |

## Open Issues with Context

### Ready for next session

| ID | P | Title | Context / Suggestions |
|----|---|-------|----------------------|
| unitAI-9d3a | P2 | sync-docs (glm-5) runs excessively long — needs budget guard | Real bug with concrete observed symptom (16min, 160 tool calls, ignored steers). Bead lists 4 candidate fixes without picking one — needs an overthinker bead first to choose between (a) `max_tool_calls` field on spec schema enforced by supervisor, (b) steer responsiveness fix in supervisor, (c) faster-model substitution, (d) session-level wall-clock budget. Note: I patched supervisor.ts twice this session (8b812 fix + 3k6sa feature) — recommend reviewer involvement on any further deep edit there. |
| unitAI-znkgi.1 | P2 | CHANGELOG.md seed — backfill from current state to v3.8.0 | Manual curation against `.xtrm/reports/2026-04-26-523fc559.md` and `.xtrm/reports/2026-04-26-2cd6af43.md`. Existing CHANGELOG.md has [Unreleased] + [3.5.0]+ but is not Keep-a-Changelog v1.1.0 conformant. Skip the parallel fln4q-epic work per bead constraints. README.md + handoff-feedor.md need one-line pointer additions. |
| unitAI-0vb3 | P2 | Design task-scoped architecture snapshot injection | Output is a proposal, not code. Six explicit questions in the bead — could go to overthinker for a tradeoff analysis between static repo map vs per-bead GitNexus query. May absorb or depend on unitAI-tz4r and unitAI-231x. |

### Backlog (no immediate action)

| ID | P | Title | Context / Suggestions |
|----|---|-------|----------------------|
| unitAI-pnqgd | P1 | Epic: continue board-wide backlog cleanup | Per its own notes, mostly drained. Active keep-open set: explorer note-append bug, research-node descendants, task-scoped architecture snapshot, observability retention, sync-docs runtime budget guard, output_type surfacing (output_type was done in dafa56f1). Likely closeable as substantially complete with a final audit pass. |
| unitAI-2b3m | P1 | Research Node epic | Big — researcher specialist + multi-agent node architecture. Substantial design + impl work. |
| unitAI-znkgi | P2 | Epic: CHANGELOG pipeline + release workflow | Parent of znkgi.1–.5 chain. Don't start until znkgi.1 (seed) is done. |
| unitAI-znkgi.2 | P2 | changelog-keeper specialist + changelog-conventions mandatory rule | Blocked on znkgi.1. |
| unitAI-znkgi.3 | P2 | sp release prepare / publish — CLI for tag-driven releases | Blocked on znkgi.2. |
| unitAI-znkgi.4 | P3 | Explore: in-CLI 'update available' nudge | Blocked on znkgi.3. |
| unitAI-znkgi.5 | P3 | Wire skill-side update awareness | Blocked on znkgi.4. |
| unitAI-kbxu7 | P4 | Remove sp poll command | Wait condition unmet — deprecation shipped *in* 3.10; need at least one minor release of soak before removal. Reconsider in 3.11+. |

### Out-of-scope follow-ups embedded in closed-bead notes

- **or6kw v2 candidates**: per-mutation `specialist_events` audit row with sha256; retain-previous-on-validation-failure (currently lazy reload throws on bad spec); mid-request integration test.
- **3k6sa docs follow-up**: compose example doc explicitly noting trust flags are NOT default and require operator opt-in.
- **rczcp re-soak**: 2026-05-05 or after ≥30 diverse-specialist jobs.

## Memories Saved

| Key | Content |
|-----|---------|
| `supervisor-ts-initial-turn-to-waiting-path-for` | initial-turn path must call client.upsertResult AND appendResultToInputBead inline (with skipFinalKeepAliveInputBeadAppend=true). Without these, sp result and bead notes stay empty until keepalive exits. handleResumeTurn ~1259-1298 is the pattern to mirror. result.ts readResultOutput should defensively fall back to latest run_complete event payload. |
| `specialist-prompts-hard-instruct-mcp-only-gitnexus-calls` | spec system+task_template content overrides mandatory-rule guidance. When adding tool-fallback to a mandatory rule, audit specialist prompts for hard-instructed MCP-only call patterns and update those too. |
| `rczcp-serena-adoption-observability-meta-event-mandatory-rul` | meta event mandatory_rules_injection.sets_loaded surfaces resolved sets accurately — but if a stale user overlay has empty mandatory_rules, resolved set list is just globals. Verify rule injection by reading specialist input transcripts directly when in doubt. |
| `specialist-tier-resolution-specialistloader-prefers-spec` | SpecialistLoader prefers .specialists/user/<name> over .specialists/default/<name> over config/specialists/<name>. Stale user overlay can silently shadow recent default fixes. sp doctor user-overlay check (added bb3h6) is the canonical detector. |
| `test-isolation-gotcha-tests-that-mutate-process-env` | Tests that mutate process.env.XDG_DATA_HOME (or similar) MUST save+restore in beforeEach/afterEach. resolveDbDirectory reads XDG_DATA_HOME and ignores cwd/gitRoot when set. Pattern in tests/unit/cli/db.test.ts is canonical. |
| `user-overlay-cleanup-pattern-deleting-specialists-user-name` | Deleting .specialists/user/<name>.specialist.json restores default-tier inheritance. sp doctor user-overlay drift check (bb3h6) is the canonical detector. Don't sync overlays automatically — operator decision. |
| `sp-serve-readiness-pattern-keep-the-readiness-evaluator` | evaluateReadiness exported as pure async function from serve.ts so it's unit-testable. State (shuttingDown, audit timestamp window, dbWriteFailuresTotal) lives in plain ReadinessState. Failure precedence: draining > degraded:audit > pi_config_unreadable > db_not_writable > user-dir checks. Audit failures wire from script-runner via onAuditFailure callback in ScriptRunnerOptions. |
| `sp-serve-trust-flags-pattern-default-reject-for` | default-reject for skills.paths, prompt.skill_inherit, skills.scripts. Three CLI flags gate them. compatGuard takes optional TrustOptions. computeSkillSources(spec) reads each resolved path and embeds {path, sha256} into status.skill_sources for audit. Unreadable files get sha256='unreadable'. |
| `sp-serve-hot-reload-pattern-v1-createuserdirwatcher-loader` | createUserDirWatcher supports native fs.watch (default) or mtime polling (--reload-poll-ms for macOS/VirtioFS). Both queue events into pending set, debounce, then call loader.invalidateCache(name) per touched basename. Per-request immutable spec naturally satisfied because runScriptSpecialist captures spec at request entry. |

## Suggested Next Priority

1. **`unitAI-9d3a`** (sync-docs runtime budget guard) — Real P2 bug with reproducible symptom. Recommend dispatching `overthinker` first to pick between the 4 candidate fixes; then a single executor pass with reviewer. The supervisor.ts edit is moderate risk given two recent edits there this session — reviewer involvement is warranted. Highest-value remaining bug.
2. **`unitAI-pnqgd`** epic audit pass — Per its own notes, mostly drained. A 30-minute audit could close it as substantially complete or surface 1–2 children worth keeping open. Cleans up the "open epic" noise on `bd ready`.
3. **`unitAI-znkgi.1`** (CHANGELOG seed) — Manual curation, ~1 hour. Unblocks the entire znkgi chain (changelog-keeper specialist, sp release CLI, update-nudge skill). Best done by a human or `sync-docs` specialist with the two referenced session reports as input.
4. **`unitAI-0vb3`** (architecture snapshot design) — Design proposal, not code. Best routed to `overthinker` for the 6-question tradeoff analysis. Output unblocks implementation work in unitAI-tz4r / unitAI-231x.
