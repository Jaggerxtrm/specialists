---
session_date: 2026-06-16
branch: master
commits: 4
issues_closed: 6
issues_filed: 7
specialist_dispatches: 10
models_used: [openai-codex/gpt-5.5, openai-codex/gpt-5.4, nano-gpt/moonshotai/kimi-k2.6:thinking, nano-gpt/moonshotai/kimi-k2.5, nano-gpt/moonshotai/kimi-k2.6, nano-gpt/minimax/minimax-m3]
handoff_bead: unitAI-1568x
---

# Session Report — 2026-06-16

## Summary

Continuation of the 2026-06-15 KAN-91 session, focused on **operator-side configuration of the global override layer + diagnosis of model fit in the specialist runtime**. Three concrete deliveries landed on master: (1) commit `c471ec46`+`fc9168e2` fixed a bead-id parser bug in the three KAN-91 live smoke harnesses (`tests/integration/cli/run-{notes-mode,fallback-chain,preset}.live.test.ts`) discovered by running `SPECIALISTS_LIVE_SMOKE=1` for the first time — the harnesses had been shipped earlier in the session and approved by reviewer because the live env flag was never set during CI; (2) commit `bd00e02c` rewrote `setup-specialists` skill v1.0→v2.0 (162→261 LOC) and added a `## Global User Override Layer (KAN-90/91)` section to `specialists-creator` v1.3→v1.4 (+118 LOC) — the v1.0 setup-specialists literally claimed "There is no supported global override layer yet" four days after KAN-90 shipped; (3) commit `3e2bf919` added the corresponding `### Changed` bullet to CHANGELOG.md `[Unreleased]` after the operator caught the omission. The orchestrator also exhaustively probed model fit in the specialist runtime across 10 dispatches: confirmed `openai-codex/gpt-5.4` works end-to-end (refuting a stale claim from another agent), proved Kimi K2.5 via nano-gpt produces a clean structured verdict (21 turns / 44 tool calls / 1711-char output on a real `service-skills-sync` audit) **when `execution.thinking_level` is left null to inherit pi's `defaultThinkingLevel: "high"` — forcing `off` produces a single-space final assistant message**, and disproved MiniMax M3 viability for agentic roles (base variant bails prematurely after announcing a pivot; `:thinking` variant goes scope-drift per operator). NVIDIA Nemotron 550B (free via NIM API) was added to `~/.pi/agent/models.json` with the operator-supplied API key but both 550B Ultra and 30B Nano Omni Reasoning are pi-incompatible at the agentic level (550B: pi can't parse the `reasoning_content` stream separation, times out 60s+; 30B: emits tool calls as `</think>` + JSON content text, not OpenAI `tool_calls`). Final user.json state: 7 specs on Kimi K2.5 with `thinking_level: null`, gpt-5.4 retained for seconder/changelog-drafter/etc., 9 Serena opt-outs + 5 GitNexus opt-outs applied. Filed Epic `unitAI-t86wh` (interactive setup-specialists v3.0) + 3 phase children covering a `sp setup` CLI verb, benchmark fetch module pinned to artificialanalysis.ai + lmarena, and an in-repo agentic-followthrough probe suite specifically designed to catch the kind of failure the m3 explorer probe surfaced (which Chatbot Arena ELO cannot).

## Issues Closed (6)

### Code work (this repo)

| ID | Title | Type | Specialist | Close context |
|---|---|---|---|---|
| `unitAI-s35zf` | Fix bead-id parser in KAN-91 live smoke harnesses | bug | executor (gpt-5.5) in worktree | 4-char regex fix replacing `stdout.trim().split(/\s+/).at(-1)` (which captured `"open"` from bd create's `Status: open` line) with `.match(/unitAI-[a-z0-9]+/)?.[0]` across 3 test files; merged via `fc9168e2`. Discovered when orchestrator first ran the harnesses with `SPECIALISTS_LIVE_SMOKE=1` after they'd been shipped and reviewer-approved (live env never set during CI). |
| `unitAI-vme0p` | Update setup-specialists + specialists-creator skills for KAN-90/91 global config | chore | orchestrator-direct (markdown only) | `setup-specialists` v1.0 → v2.0: full rewrite, replaces literal false claim "There is no supported global override layer yet" with the 3-layer merge + `sp init/edit/doctor --global` workflow + 15-row allowlist table + preset refs + handoff modes section + pitfalls. `specialists-creator` v1.3 → v1.4: additive `## Global User Override Layer (KAN-90/91)` section (+118 LOC). Commit `bd00e02c`. |
| `unitAI-2k0ud` | CHANGELOG entry for the skill rewrite | chore | orchestrator-direct | Operator caught omission at end-of-session. Added 1 bullet under `[Unreleased] ### Changed` citing `unitAI-vme0p`. Commit `3e2bf919`. Memory `changelog-entry-policy-skill-rewrites-ARE-user-facing-2026-06-16` documents the corrected classification rule. |

### Probe beads (model fit diagnostics)

| ID | Title | Outcome | Notes |
|---|---|---|---|
| `unitAI-bsdja` | gpt-5.4 seconder sanity check on CHANGELOG.md | PASS | 4 turns / 6 tool calls / 20s / 9256 tok / 840-char structured JSON with all 5 required seconder fields. Disproves the "gpt-5.4 doesn't work" claim from another agent. |
| `unitAI-9jaye` | minimax-m3 seconder sanity check on CHANGELOG.md | PASS (single-turn) | 1 turn / 0 tool calls / 10s / 750-char structured JSON. ~2× faster than gpt-5.4 on the same task. M3 base works for single-turn verdict roles with complete input. |
| `unitAI-6gnvx` | minimax-m3 explorer probe — map the 3-layer field merge | FAIL (premature bail) | 14s / 2 turns / 2 tool calls / 127-char meta-comment. Model announced a pivot ("I'll work with the working directory directly...") then `agent_end` without executing it. Opposite of the operator-observed `:thinking` variant failure (scope-drift / 150k tok wandering). |

## Issues Filed (7)

| ID | P | Type | Title | Why |
|---|---|---|---|---|
| `unitAI-o5rwj` | P2 | bug | Live smoke harnesses authored without execution — 2 design bugs (KAN-91 follow-up) | After the parser fix unblocked execution, the harnesses revealed 2 deeper design bugs: (a) `run-fallback-chain.live.test.ts`+`run-preset.live.test.ts` assert `smoke.stdout.trim()` matches a 6-char hex job id, but `sp run` is dispatched WITHOUT `--background` so no job id is printed; (b) `run-notes-mode.live.test.ts` overrides `HOME=tempHome` which blanks out `~/.pi/` and breaks pi auth. Diagnosis precise enough that a fix specialist can land it in one turn. |
| `unitAI-61h1b` | P2 | bug | sp edit --global positional form falls through to vim | `sp edit --global <name>.<dot.path> <value>` is documented as valid in `sp edit --help` but actually opens `$EDITOR` (vim) instead of consuming the positional args. Cost ~3 minutes hung subprocess + a stale `.user.json.swp` lock file mid-session. The `--set` form works correctly — use it in scripts and one-liners. |
| `unitAI-t86wh` | P2 | epic | [EPIC] Interactive setup-specialists v3.0: benchmark-driven model assignment | Parent epic for the 3-phase upgrade of setup-specialists from one-off docs skill to interactive workflow with benchmark-grounded recommendations. Motivated by today's discovery that model attempts repeatedly burned (Kimi/MiniMax/Nemotron) because there's no reproducible workflow that probes models before applying. |
| `unitAI-oeysi` | P2 | task | [Phase C] benchmark module + agentic-followthrough probe suite | Foundation child of `t86wh`. Implements `src/specialist/benchmarks.ts` (pinned to artificialanalysis.ai + lmarena, 24h cache TTL, 14-day freshness window, offline-graceful) and `src/specialist/model-probes.ts` (the agentic-followthrough probe whose absence allowed today's m3 explorer failure to slip through). `READY` — no deps. |
| `unitAI-jy8b7` | P2 | task | [Phase B] sp setup CLI verb — discovery + plan + apply | Middle child of `t86wh`. New `sp setup [--discovery|--fetch-benchmarks|--plan|--apply|--probe-only|--interactive]` verb consuming Phase C primitives. `BLOCKED by oeysi`. |
| `unitAI-hdrr6` | P2 | task | [Phase A] setup-specialists v3.0 — interactive skill with AskUserQuestion checkpoints | Skill child of `t86wh`. v3.0 rewrite that drives the orchestrator through 5 phases via `AskUserQuestion` checkpoints. `BLOCKED by jy8b7`. |
| `unitAI-4wlf3` | P4 | task | Investigate why pi 'tools: yes' metadata doesn't match agentic reality for some models | Captures 3 confirmed cases: (a) `nvidia/nemotron-3-nano-omni-30b-a3b-reasoning` tools=yes but emits `</think>` + JSON-in-content (DSML-pattern); (b) `nano-gpt/deepseek/deepseek-v4-*:thinking` similar (memory `model-incompat-deepseek-v4-dsml-via-nano-gpt`); (c) `nvidia/nemotron-3-ultra-550b-a55b` with `reasoning_content` stream pi doesn't parse. Backlog research — no source change required, but informs future skill version of Phase C probe data. |

## Specialist Dispatches

### Wave 1 — Production: live smoke parser fix (this repo)

| Job | Specialist | Model | Bead | Outcome |
|---|---|---|---|---|
| `259af7` | executor | openai-codex/gpt-5.5 | unitAI-s35zf | Landed parser fix in 3 test files, ran live smoke (which surfaced the o5rwj design bugs), committed `c471ec46`, pushed feature branch. Merged via manual `git merge --no-ff` to master as `fc9168e2`. |

### Wave 2 — Model probes (this repo)

| Job | Specialist | Model | Bead | Outcome |
|---|---|---|---|---|
| `f1bcad` | seconder | openai-codex/gpt-5.4 | unitAI-bsdja | PASS — 20s, structured verdict, gpt-5.4 works end-to-end |
| `e0ecba` | seconder | nano-gpt/minimax/minimax-m3 | unitAI-9jaye | PASS — 10s, single-turn structured verdict |
| `67450b` | explorer | nano-gpt/minimax/minimax-m3 | unitAI-6gnvx | FAIL — premature bail, 14s, no answers to the 5 SCOPE questions |

### Wave 3 — Model probes (mercury/infra cross-repo)

All against `~/projects/mercury/infra` for `service-skills-sync` audit of `promtail/SKILL.md`. Beads tracked in that repo's bd db (`infra-*`), referenced here for handoff completeness.

| Round | Job | Model | Outcome | Diagnostic |
|---|---|---|---|---|
| 1 | `b33629` | nano-gpt/moonshotai/kimi-k2.6:thinking | FAIL (text=`" "`) | 3 turns / 4 tool calls / 19s — final text 1 char |
| 2 | `9891c4` | nano-gpt/moonshotai/kimi-k2.6 (no :thinking, default pi thinking=high) | FAIL (text=`" "`) | 8 turns / 15 tool calls / 31s — workflow executed correctly but final synthesis was 1 char |
| 3 | `eaa490` | nano-gpt/moonshotai/kimi-k2.6 + `thinking_level=off` override | CRASH at 134s | 15 turns / 46 tool calls — orchestrator-side `thinking_level=off` override caused crash; this was the misclassification root cause |
| 4 | `89a13c` | nano-gpt/moonshotai/kimi-k2.5 + `thinking_level=off` carried over | bail at 9s | 1 turn / 0 tool calls — same orchestrator-side override still in place |
| 5 | `49e986` | nano-gpt/moonshotai/kimi-k2.5 + `thinking_level=null` (cleared) | PASS | 21 turns / 44 tool calls / 108s / 1711-char structured verdict + 1848-char auto-appended bead notes. Operator's intuition correct: K2.5 always worked when default pi thinking was preserved. |

### Wave 4 — Side probes (no bead — `pi --print` direct)

Direct `pi --print --model <…>` ping + 2-tool multi-call probes against: `nano-gpt/minimax/minimax-m2.7` (3 thinking levels — all OK on simple, but operator confirms weak/old on real work); `nano-gpt/minimax/minimax-m3` (base + thinking off — OK); `nvidia/nvidia/nemotron-3-ultra-550b-a55b` (timeouts 60s+ even with `--thinking off`); `nvidia/nvidia/nemotron-3-nano-omni-30b-a3b-reasoning` (ping OK, multi-tool returns `</think>` + JSON description as text content, no actual tool execution).

## Problems Encountered

| Problem | Root Cause | Resolution |
|---|---|---|
| Live smoke harnesses (`run-*.live.test.ts`) shipped with bd-id parser bug; reviewer + test-runner approved them without ever running them live | The tests are gated by `SPECIALISTS_LIVE_SMOKE=1` env flag; CI doesn't set it. The test-engineer wrote a parser that would NEVER work but the failure path was unreachable in green CI. | Fixed in `unitAI-s35zf`. Lesson: gated tests have hidden contracts that survive review. Filed `o5rwj` for the deeper design bugs the parser fix unblocked. |
| Misclassified Kimi K2.5 as "structurally broken via nano-gpt" after 4 failed probes | Round-3 troubleshooting set `execution.thinking_level=off` in `~/.config/specialists/user.json` for `service-skills-sync` to "fix" an empty-output hypothesis. The override persisted through rounds 4 and 5a without being cleared, even when the model was swapped to K2.5. Operator correctly insisted "il problema è qualcosa che hai combinato oggi". | Cleared override → K2.5 round-5b passed with 1711-char structured verdict. Memory `kimi-via-nano-gpt-CORRECTED-2026-06-16` updated to reflect that Kimi works fine when `thinking_level` is null. New rule: **never force `thinking_level=off` on a thinking-class model without a documented reason** (pi default `defaultThinkingLevel: "high"` is correct for them). |
| Spent significant time searching for an "append previous message to bead" PR/feature that the operator believed had shipped | The feature DID ship but under a different name: `notes_mode: "full-trail" \| "final-only"` (unitAI-10y07 commit `44b2c7bd`). I was grepping for `previous_message`/`append_previous`/`prev_message` keywords that don't appear in the codebase. | Operator pointed at the actual commits + code locations (`schema.ts:130`, `launch.ts:62`, `supervisor.ts:208`). Memory `handoff-persistence-3-state-model-unitai-10y07-builds` updated from "in progress" to "shipped" with the correct field name. |
| `setup-specialists` v1.0 SKILL.md literally claimed "There is no supported global override layer yet" — false since KAN-90 shipped 4 days earlier | The KAN-90 source-shipping PRs (#116/#117) and the KAN-91 follow-up phases all touched code + docs/upgrade-notes but the OPERATOR-FIRST skill (the one users read via `/setup-specialists`) was never updated in the same commit. The false claim was the operator's first impression for 4 days. | Rewrote setup-specialists v2.0 (`bd00e02c`) + filed memory `skill-drift-on-shipped-config-features` documenting the process rule: any PR that adds/extends an operator-facing CLI surface MUST update the corresponding skill in the same commit. |
| Misclassified the skill rewrite as "agent-facing only / skip CHANGELOG" | Too-conservative reading of session-close-report Step 6's exclusion of "skill or memory edits that only affect agents". Operator-facing skills exposed via slash commands ARE user-facing. | Operator caught the omission ("come fai a dire che niente è user facing?"). Added the entry retroactively in `unitAI-2k0ud` (commit `3e2bf919`). Memory `changelog-entry-policy-skill-rewrites-ARE-user-facing-2026-06-16` documents the corrected rule. |
| NVIDIA Nemotron 550B Ultra times out via `pi --print` despite working via direct API curl | Pi's stream parser does not extract NVIDIA NIM's separate `reasoning_content` delta field; pi waits for `content` chunks which never arrive within timeout because the model exhausts its reasoning budget in the reasoning_content stream first. Verified by streaming curl directly: `{"delta":{"reasoning":"...","reasoning_content":"..."}}` chunks land before any content. | Filed `unitAI-4wlf3` P4 for upstream investigation. Provider entry kept in `~/.pi/agent/models.json` (operator-supplied API key) for potential future pi versions that fix the parser. No specialist assigned to Nemotron. |
| MiniMax-M3 :thinking (operator's prior experience) wanders 150k tok scope-drift; M3 base (today's probe) opposite-fails by bailing after announcing pivot | Both variants show poor agentic loop discipline despite passing simple `pi --print` 2-tool probes. M3 base saw `gitnexus_list_repos` return an unexpected result (no `specialists` repo in index because mercury/infra has a different gitnexus name), announced a pivot to use bash, then `agent_end` without executing the pivot. | Restored explorer to `nano-gpt/moonshotai/kimi-k2.5`. Memory `minimax-m3-base-agentic-failure-mode-2026-06-16` documents the exact failure shape. M3 entry stays in models.json for future single-turn-verdict experiments. |
| Two background `kill -9` operations on `sp edit` hung-on-vim processes | Same root cause as `unitAI-61h1b` filed bug — `sp edit --global <name>.field value` without `--set` falls into `$EDITOR` mode in non-TTY context. Stale `.user.json.swp` left behind. | Killed manually with `kill -9 <pid>`. Removed `.swp` with `rm -f`. All subsequent `sp edit --global` calls switched to the `--set` form. |

## Code Changes

### Skill rewrites (`config/skills/`)

- `config/skills/setup-specialists/SKILL.md` — full rewrite v1.0 (162 LOC) → v2.0 (261 LOC). Removes the false "no global layer yet" claim. New sections: "The 3-layer field merge", "When to use --global vs per-repo", "Workflow" (5 steps from `sp init --global` through `sp doctor --specialists` verification), "Allowlist table" (15 user-environment fields with type + notes), "Preset references (KAN-91 Phase 3)", "Handoff modes" (`full-trail` vs `final-only` with the chained-pipeline output_file recipe), "Common pitfalls" (positional form vim fallthrough, `thinking_level: off` breaking thinking-class models, provider auth gaps, repo-override shadowing), "Reference files" table, "Report template".
- `config/skills/specialists-creator/SKILL.md` — additive `## Global User Override Layer (KAN-90/91)` section between `Schema Reference` and `Built-in Template Variables` (+118 LOC). Documents the 3-layer merge, CLI surface, `OVERRIDE_ALLOWED_*` allowlist mapping (`OVERRIDE_ALLOWED_EXECUTION_FIELDS` + `OVERRIDE_ALLOWED_NESTED_EXECUTION_PATHS` + `OVERRIDE_ALLOWED_PROMPT_FIELDS` + `OVERRIDE_ALLOWED_TOP_FIELDS`), preset refs syntax, `_doc` sentinel cycle, `thinking_level: off` pitfall. Cross-refs the existing per-spec schema reference: same fields work globally with the same semantics, only the scope changes.

### Test fix (`tests/integration/cli/`)

- 3 live smoke harnesses (`run-notes-mode.live.test.ts`, `run-fallback-chain.live.test.ts`, `run-preset.live.test.ts`) — bd-id extraction changed from `stdout.trim().split(/\s+/).at(-1) ?? ''` to `stdout.match(/unitAI-[a-z0-9]+/)?.[0] ?? ''`. Fixes 4 fixture assertion failures observed when env-gated tests were run live for the first time.

### CHANGELOG

- `CHANGELOG.md` `[Unreleased] ### Changed` — added bullet covering the setup-specialists v2.0 + specialists-creator v1.4 skill rewrite, citing `unitAI-vme0p`, scope summary (3-layer merge teaching, sp init/edit/doctor --global workflow, allowlist coverage, preset refs, handoff modes, pitfalls).

### Local config (NOT committed — `~/.config/specialists/user.json`)

Operator-environment changes only, surfaced here for handoff:
- 30/31 specialists configured (only `bare` template remains null, as expected).
- 7 specs on `nano-gpt/moonshotai/kimi-k2.5` with `thinking_level: null`: `service-skills-sync, changelog-keeper, sync-docs, smoke-echo, test-runner, transcriber, explorer`.
- `seconder`, `executor`, `debugger`, `reviewer`, `code-sanity` (et al) on `openai-codex/*` fleet.
- Serena opt-out (9): overthinker, planner, changelog-drafter, changelog-keeper, memory-processor, researcher, transcriber, obligations-scanner, explorer.
- GitNexus opt-out (5): changelog-drafter, changelog-keeper, researcher, transcriber, memory-processor.

### Pi config (NOT committed — `~/.pi/agent/models.json`)

- New provider entry `nvidia` with operator-supplied API key (`nvapi-…` prefix), endpoint `https://integrate.api.nvidia.com/v1`, OpenAI-completions API. Pi already enriches the model list dynamically from NVIDIA NIM `/v1/models` once the key is present.
- New model entries: `nano-gpt/minimax/minimax-m3` (base, no `:thinking`) and the NVIDIA Nemotron entry (550B Ultra). Both kept in place for future probing; neither assigned to any specialist as of session end.

## Documentation Updates

- **Skills**: `setup-specialists` v1.0 → v2.0, `specialists-creator` v1.3 → v1.4. Source-of-truth in `config/skills/` (mirror at `.xtrm/skills/active/` propagates via symlinks automatically).
- **CHANGELOG.md**: 1 new bullet under `[Unreleased] ### Changed` for the skill rewrite. No release was cut this session.
- **Memories saved this session**: 9 (full list in §Memories Saved).
- **No `docs/` change**: KAN-90/91 upgrade notes (`docs/upgrade-notes/kan-90-*.md`, `docs/upgrade-notes/kan-91-*.md`) already existed and remain accurate; the skill rewrite cross-references them.
- **No `CLAUDE.md` change**: today's work didn't add/remove services, change tool wiring, or change top-level workflow commands.
- **No `sync-docs` dispatch**: no source files under a service-skills-registered area changed (skill files and tests only).

## Open Issues with Context

### Filed this session — ready for next pickup

| ID | P | Title | Status | Context / Suggestions |
|---|---|---|---|---|
| `unitAI-oeysi` | P2 | [Phase C] benchmark module + agentic-followthrough probe suite | open / **READY** | Foundation of Epic `t86wh`. No deps. Implement `src/specialist/benchmarks.ts` (artificialanalysis.ai + lmarena, 24h TTL, 14-day freshness, offline-graceful) and `src/specialist/model-probes.ts` (the agentic-followthrough probe — 3-question SCOPE-bounded explore task, classify PASS / PARTIAL / FAIL on turns × tool calls × output_length × scope_drift). Probe must run in isolated worktree and NEVER write outside `~/.cache/specialists/probes/<probe-id>/`. Bead has full success criteria + validation table. |
| `unitAI-jy8b7` | P2 | [Phase B] sp setup CLI verb | open / blocked-by `oeysi` | New top-level verb `sp setup [--discovery\|--fetch-benchmarks\|--plan\|--apply\|--probe-only\|--interactive]`. All sub-modes must support `--json`. `--apply` must be atomic (snapshot/rewrite/fsync/rename) and idempotent. No interactive TTY prompts — operator interactivity is the orchestrator's job via Phase A skill + `AskUserQuestion`. |
| `unitAI-hdrr6` | P2 | [Phase A] setup-specialists v3.0 — interactive skill | open / blocked-by `jy8b7` | Skill rewrite v2.0 → v3.0 that drives the orchestrator through 5 phases (DISCOVERY / FETCH / INTERACTIVE Q / PROPOSE / APPLY) via explicit `AskUserQuestion` checkpoints. Specifies exact question wording + option labels + expected parse contract for reproducibility across agents. Preserves the v2.0 reference content (allowlist table, handoff modes, pitfalls) verbatim; the interactive flow is additive. |
| `unitAI-o5rwj` | P2 | Live smoke harnesses authored without execution — 2 design bugs | open | After the `s35zf` parser fix unblocked execution, 2 deeper bugs surfaced. Bug 1: `run-fallback-chain.live.test.ts:127-128` and `run-preset.live.test.ts:147-148` assert `smoke.stdout.trim()` matches `/^[a-f0-9]{6}$/` but `sp run` is dispatched without `--background` so no job id is printed. Fix path: add `--background` and adjust the downstream waiter, OR drop the regex assertion and assert side-effects on the bead (notes, sp ps records, observability events). Bug 2: `run-notes-mode.live.test.ts:64-65` overrides `HOME=tempHome` which blanks out `~/.pi/` and breaks pi auth. Fix path: bind-mount/symlink real `~/.pi` into tempHome, OR use `XDG_CONFIG_HOME` to scope only the specialists global config. |
| `unitAI-61h1b` | P2 | sp edit --global positional form falls through to vim | open | `sp edit --global <name>.<dot.path> <value>` (no `--set`) opens `$EDITOR` instead of consuming positional args. Docs (`sp edit --help`) show the form as valid. Either fix the parser to bind positional args after `--global`, or mark the form as deprecated in help. Non-TTY callers should NEVER drop into `$EDITOR` — error out or print a hint. |
| `unitAI-4wlf3` | P4 | Investigate pi 'tools: yes' metadata vs agentic reality mismatch | open / backlog | Captures 3 confirmed cases (nemotron 30B nano omni reasoning + deepseek-v4 DSML + nemotron 550B reasoning_content). Backlog research — investigate source of pi metadata (hardcoded? heuristic? model card?), and whether pi could/should add per-provider format translation for known non-OpenAI tool patterns. Findings should inform Phase C skill version of the probe data. |
| `unitAI-t86wh` | P2 | [EPIC] Interactive setup-specialists v3.0 | open | Parent epic. Tracks C → B → A. Final deliverable is operators going from `npm install -g @jaggerxtrm/specialists` to working configured fleet in <5 minutes with benchmark-grounded model assignments. |

### Open backlog (touched indirectly, not actioned this session)

The full open-bead list (~45 items) is captured by `xt report generate` and elided here. The items most relevant to today's work that warrant explicit mention for the next pickup:

| ID | P | Title | Why mentioned |
|---|---|---|---|
| `unitAI-c4g0m` | P0 | LSP overhead: pool per-repo, skip for short roles, daemonize serena | Related to today's Serena opt-out in user.json (9 specs) — a longer-term fix would reduce the need for opt-outs entirely. |
| `unitAI-ucpcy` | P1 | sp init --global should not bake repo-local .specialists/user/ specs into the global file | Today's session used `sp init --global` extensively; this bead documents a known gotcha that doesn't block but should be fixed before the v3.0 Phase A skill rewrite calls `sp init --global` interactively. |
| `unitAI-aav4w` | P2 | Backfill KAN-90 in canonical reference docs (README, cli-reference, features, ARCHITECTURE) | Today fixed the SKILLS; the canonical reference docs (README et al.) likely have the same staleness. The v3.0 skill rewrite is a partial fix; this bead covers the rest. |

The other ~40 open beads from the skeleton (substrate work, AgentOps telemetry, sp merge rework, etc.) are pre-existing tracks unchanged by this session — see prior session reports for their handoff context.

## Memories Saved

| Key | Content (summary) |
|---|---|
| `handoff-persistence-3-state-model-unitai-10y07-builds` (UPDATED) | Corrected the stale "in progress" memory: the 3-state handoff persistence (notes_mode + output_file unification) SHIPPED on 2026-05-29 via commits 44b2c7bd / 8906a92d / 428cd7f7 / 0c900174 / unitAI-f58ma. Includes the exact field-references (schema.ts:130 / global-config.ts:106 / launch.ts:62 / supervisor.ts:208 / shouldPersistHandoffBlock + formatHandoffBlock). |
| `skill-drift-on-shipped-config-features-when-a` | New rule: when a framework-level CLI surface (e.g. `sp init/edit/doctor --global`) ships, the operator-first skills (setup-specialists, specialists-creator) MUST be updated in the same PR or commit. setup-specialists v1.0 lived for 4 days post-KAN-90 with a literal false claim because no test/CI catches stale skill content. |
| `kimi-k2-6-thinking-via-nano-gpt-empty` (REPLACED via `bd remember --key`) | Final state: Kimi K2.5 via nano-gpt **WORKS** in specialist runtime when `execution.thinking_level=null` (inherits pi `defaultThinkingLevel: "high"`). Forcing `off` produces empty assistant text (single space) after multi-tool runs. Probe `49e986` (bead `infra-hti6`) on mercury/infra service-skills-sync: 21 turns / 44 tool calls / 108s / 1711-char structured verdict + 1848-char bead notes auto-append. Earlier rounds 1-4 failures were all caused by orchestrator's `thinking_level=off` override carried over without being cleared. |
| `gpt-5-4-via-openai-codex-works-2026` | gpt-5.4 verified working end-to-end in specialist runtime (probe `f1bcad`, seconder on CHANGELOG.md, 4 turns / 6 tool calls / 20s / 840-char structured JSON). Disproves any "gpt-5.4 doesn't work" claim. Includes the debug checklist for when another agent reports the model as broken: (1) check `thinking_level=off` override, (2) verify pi openai-codex auth, (3) verify the verdict isn't being misread as a crash. |
| `minimax-on-nano-gpt-thinking-vs-base-2026` | M2.7 is weak/old (operator: "non sta funzionando bene") across all thinking levels — not a thinking config issue. M3 `:thinking` variant has `reasoning=true tools=no` per pi metadata — unusable for specialists. M3 base (added to `~/.pi/agent/models.json` as a new entry; nano-gpt accepts it; pi can spawn it) supports tool calling and produces valid structured JSON on single-turn tasks. |
| `minimax-m3-base-agentic-failure-mode-2026-06` | M3 base FAILS on agentic multi-turn tasks despite passing single-turn probes. Probe `67450b` (explorer): 14s / 2 turns / 2 tool calls — announced "I'll work with the working directory directly" on turn 2 but emitted `agent_end` WITHOUT executing any bash/read tools. Opposite failure to the `:thinking` variant (operator-observed 150k tok scope-drift). Both unsafe for agentic roles; M3 base possibly OK as single-turn verdict gate. |
| `nemotron-via-nvidia-nim-pi-incompatible-2026-06` | Both NVIDIA Nemotron variants tested via pi-coding-agent fail differently. 550B Ultra: pi metadata reports tools=no AND pi cannot parse the model's `reasoning_content` stream separation (timeouts 60s+). 30B Nano Omni Reasoning: pi metadata reports tools=yes BUT model emits tool calls as `</think>` + JSON content text instead of OpenAI `tool_calls` field. Direct API curl confirms the model behavior; the failure is in pi's parser. Provider entry kept in `~/.pi/agent/models.json` for future pi versions. |
| `changelog-entry-policy-skill-rewrites-are-user-facing` | Operator-facing skills exposed via slash commands like `/setup-specialists` or `/specialists-creator` ARE user-facing for CHANGELOG purposes. The session-close-report skill exclusion of "skill or memory edits that only affect agents" applies ONLY to skills/memories private to LLM agents (internal mandatory_rules, runtime context injections), NOT to skills humans read during setup. Rule: when a skill rewrite (a) replaces a previously-published false claim, (b) introduces a new interactive workflow, or (c) is the canonical doc for a shipped CLI surface — add a `### Changed` entry citing the bead + version bump. |
| `test-engineer-bd-id-parser-gotcha-when-test` | When test-engineer authors fixtures that call `bd create` via spawnSync, do NOT parse the bead id with `stdout.trim().split(/\s+/).at(-1)` — that captures `"open"` from `bd create`'s `Status: open` tail line. Use regex `/unitAI-[a-z0-9]+/`. Shipped silently in `unitAI-gp7nq.8/.9/.10` because tests are gated behind `SPECIALISTS_LIVE_SMOKE=1` and test-runner/reviewer never executed them; fixture bug surfaced only when orchestrator ran live with the env flag. |

## Suggested Next Priority

1. **`unitAI-oeysi` ([Phase C] benchmark module + agentic-followthrough probe suite)** — `READY`, foundation of the v3.0 epic. The probe-suite design directly addresses the gap exposed by today's m3 explorer failure (model passes Chatbot Arena ELO and `pi --print` 2-tool probes but bails on real agentic flow). Landing this unblocks Phases B and A and gives every subsequent model-evaluation session a reproducible verdict instead of an opinion.
2. **`unitAI-o5rwj` (live smoke harness design bugs)** — small surface (3 test files, 2 distinct fixes), high signal (gets the KAN-91 live smoke harnesses actually working as a CI hook). Diagnosis is precise enough that a fix specialist can land it in one turn. Should land before any further model-fit probes that want to use the live smoke harness as the verification path.
3. **`unitAI-61h1b` (sp edit --global vim fallthrough)** — recurring papercut today (2 hung subprocesses + stale swap file). Cheap fix in `src/cli/edit.ts` parser. Worth doing before the v3.0 epic Phase B starts so the new `sp setup --apply` doesn't reproduce the same gotcha.
4. **`unitAI-aav4w` (Backfill KAN-90 in canonical reference docs)** — adjacent to today's skill rewrite. The skills are now correct but the README + `docs/cli-reference.md` + `docs/features.md` + `docs/ARCHITECTURE.md` likely still have the same staleness. Worth a sweep before any new KAN-91 user lands in the repo.
