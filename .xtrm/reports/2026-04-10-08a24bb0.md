---
session_date: 2026-04-10
branch: master
commits: 81
issues_closed: 4
issues_filed: 3
specialist_dispatches: 8
models_used: [qwen3.5-plus, gpt-5.4, gpt-5.3-codex, claude-sonnet-4-6]
session_2_date: 2026-04-11
session_2_issues_closed: 14
session_2_issues_filed: 12
session_2_specialist_dispatches: 18
session_2_models_used: [qwen3.5-plus, gpt-5.3-codex, gpt-5.4, glm-5]
---

# Session Report — 2026-04-10

## Summary

Implemented `unitAI-8zui`: coordinator active orchestration — the node coordinator was a passive poller that called `sp node complete` without ever reading what members produced. The fix had three layers: (1) added `sp node result --node <id> --member <key> [--full] [--json]` CLI command to `src/cli/node.ts` with SQLite + result.txt fallback lookup, 4k preview truncation, structured JSON output, and proper exit codes; (2) updated `src/specialist/node-contract.ts` renderers (`renderForSystemPrompt`, `renderForFirstTurnContext`, `renderForDocs`) to require phase-boundary synthesis — the SSoT now mandates that `wait-phase` + `result --full` are always used as a pair; (3) added `sp steer <job_id>` and `sp resume <job_id>` to the coordinator's known command surface in `config/skills/using-nodes/SKILL.md` and the coordinator system prompt, with a steering sequence example showing read-explorer → steer-researcher. Also removed the dead `triggers` block from `config/nodes/research.node.json` (it was never parsed by the runtime). Key discovery during implementation: `sp node steer <node-id>` steers the **coordinator**, not members — the name implies member targeting but it writes to the coordinator FIFO. Member steering uses `sp steer <job_id>` directly, with `job_id` available in the resume payload member registry. The `sp node result` command was one of two valid approaches from the original issue; the alternative (just documenting `sp result <job_id>` directly) would also work since coordinators already have job IDs in their resume payload — but `sp node result` provides stable member-key addressing independent of job ID tracking.

## Issues Closed (4 this session)

| ID | Title | Type | Specialist | Close Reason |
|----|-------|------|------------|--------------|
| unitAI-8zui | Coordinator must actively orchestrate: read member output, steer, synthesize, manage phases | feature | executor b72a5f | sp node result added, synthesis mandate in skill+prompt, steer/resume documented, research.node.json triggers removed. tsc+lint pass. |
| unitAI-k1oa | Implement: coordinator active orchestration (skill + CLI + config) | task | executor b72a5f | Implemented by executor, closed on completion. |
| unitAI-f280 | Explore: node coordinator current state — skill, CLI surface, config | task | explorer 2d7800 | Exploration complete, findings fed to design. |
| unitAI-z5nh | Design: coordinator synthesis, phase management, steering protocol | task | overthinker d34f6b | Design complete, executor implemented per spec. |

## Issues Filed

| ID | P | Type | Title | Why |
|----|---|------|-------|-----|
| unitAI-f280 | P1 | task | Explore: node coordinator current state — skill, CLI surface, config | Needed ground-truth mapping before implementation: which commands exist, what's in the skill, what the resume payload contains. |
| unitAI-z5nh | P1 | task | Design: coordinator synthesis, phase management, steering protocol | Non-trivial architectural decision: sp node result vs sp result, whether result belongs in action schema, how steer semantics map to coordinator vs member. Overthinker needed. |
| unitAI-k1oa | P1 | task | Implement: coordinator active orchestration (skill + CLI + config) | Implementation tracking bead for the executor worktree run. |

## Specialist Dispatches

| Wave | Job | Specialist | Model | Bead | Elapsed | Outcome |
|------|-----|-----------|-------|------|---------|---------|
| 1 | 2d7800 | explorer | qwen3.5-plus | f280 | 1m46s | DONE — mapped 6 files, identified missing sp node result, dead triggers config, steer semantic confusion |
| 2 | d34f6b | overthinker | gpt-5.4 | z5nh | 5m31s | DONE — chose sp node result over direct sp result, kept result out of action schema, flagged steer self-loop risk |
| 3a | e3c827 | executor | gpt-5.3-codex | k1oa | 5m37s | FAILED — 0 tokens every turn, backend broken |
| 3b | 3d8654 | executor | claude-sonnet-4-6 | k1oa | 52s | FAILED — same 0-token issue, different model |
| 3c | b72a5f | executor | gpt-5.4 | k1oa | 18m4s | DONE — implemented all 5 files, tsc+lint pass, committed efdc5927 |
| 4a | d332c0 | reviewer | gpt-5.3-codex | — | 1m7s | PARTIAL — read stale worktree, missing 21e4de62 commit |
| 4b | e57c22 | reviewer | gpt-5.3-codex | — | 40s | PASS 94 — code audit: handleNodeResult, actionTypeSchema, research.node.json, index.ts |
| 4c | 20d2fd | reviewer | gpt-5.3-codex | — | 39s | PASS 94 — docs audit on master: SKILL.md, coordinator prompt, default sync |

### Problems

| Problem | Root Cause | Resolution |
|---------|-----------|------------|
| gpt-5.3-codex executor: 0 tokens every turn | Backend broken at that time | Switched to gpt-5.4 via `specialists edit executor --preset power` |
| claude-sonnet-4-6 executor: same 0-token issue | Same Pi session exit pattern, different backend | Same resolution — gpt-5.4 worked |
| `sp merge k1oa --rebuild` reported success but commit not on master | sp merge ran merge in-process but didn't update the working tree HEAD | Manual `git merge feature/k1oa-executor` |
| Reviewer 2 (6b380b) returned PARTIAL on docs | Reviewer entered stale worktree via `--job b72a5f`; worktree branch missing commit 21e4de62 (steer/resume, added directly to master) | Re-ran reviewer (20d2fd) with `--prompt` targeting master files directly — PASS |
| Merge conflict in SKILL.md + coordinator JSON | Concurrent sync-docs job (`feature/unitAI-ywsr-sync-docs`) merged while we were working | Resolved by keeping HEAD (our changes), theirs was missing steer/resume additions |

## Code Changes

### This session's scope (unitAI-8zui)

**`src/cli/node.ts`** — Added `result` to `NodeCommand` type, `parseNodeArgs` validation for `--member`/`--member-key`/`--full`/`--json`, `readPersistedJobResult()` helper (SQLite primary, result.txt fallback), `handleNodeResult()` handler with 4k preview truncation, structured JSON output, descriptive error cases (node missing, member missing, no job_id, no result) all exiting with code 1.

**`src/specialist/node-contract.ts`** — Updated all three renderers to include `sp node result --node $SPECIALISTS_NODE_ID --member <key> --full --json` and the synthesis mandate: "after wait-phase, read every participating member result before deciding next phase or completion." `actionTypeSchema` unchanged — result stays read-only CLI, not a side-effect action.

**`config/skills/using-nodes/SKILL.md`** — Added `sp node result`, `sp steer <job_id>`, `sp resume <job_id>` to command table. Added Phase planning and synthesis section with full loop, synthesis mandate, and steering guidance with concrete command examples. Added Sequence B showing read-explorer → steer-researcher pattern. Documented `sp node steer` as operator-only with explicit warning.

**`config/specialists/node-coordinator.specialist.json`** + **`.specialists/default/`** — Updated system prompt command list to include `sp steer`/`sp resume`, added execution loop steps 4-5 (read results, synthesize), added few-shot example of reading explorer result then resuming researcher with specific findings. Added explicit NEVER callout for `sp node steer`.

**`config/nodes/research.node.json`** — Removed `triggers` block (`on_start`, `on_member_waiting`, `on_all_members_waiting`) which was dead config never parsed by the runtime. Static members (explorer, overthinker, researcher) preserved as capability manifest — they define what specialists are available for the coordinator to spawn, not pre-seeded execution.

## Documentation Updates

- `config/skills/using-nodes/SKILL.md` — major update: 141 → 320 lines, phase planning, synthesis mandate, steer/resume commands, Sequences A/B/C
- `config/specialists/node-coordinator.specialist.json` — system prompt now includes full command surface and synthesis loop
- `.specialists/default/node-coordinator.specialist.json` — kept in sync

**Memory saved:**
- `sp-node-steer-node-id-message-steers-the` — `sp node steer <node-id> <message>` steers the coordinator (writes to coordinator FIFO), not a member. Future member-targeted steering needs a new command or `sp steer <job_id>` directly.

## Open Issues with Context

### Ready for next session

| ID | P | Title | Context / Suggestions |
|----|---|-------|----------------------|
| unitAI-96qy | P1 | E2E: node coordination runtime — full lifecycle with real coordinator + members | Now that coordinator has result/steer/resume in its skill, this E2E test would validate the full loop actually works: spawn explorer, wait-phase, read result, steer researcher, synthesize, complete. Run `sp node run config/nodes/research.node.json --bead <id>` and watch coordinator behavior. |
| unitAI-2b3m | P1 | Research Node: researcher specialist + multi-agent node architecture | research.node.json is now cleaner (no dead triggers). The coordinator knows how to steer/resume. Missing piece: test a real research node run to see if coordinator actually uses the new guidance. The initialPrompt was also strengthened this session. |
| unitAI-ywsr | P2 | Sync docs: cancelled status, sp stop/merge changes, crashRecovery reconciliation | sync-docs job was running concurrently this session and caused merge conflicts. The job itself (7f0940) completed but the merge result was partially overwritten when we resolved conflicts by keeping HEAD. May need a fresh sync-docs pass on the node docs specifically. |

### Backlog

| ID | P | Title | Context / Suggestions |
|----|---|-------|----------------------|
| unitAI-aq1k | P1 | Eliminate --no-worktree flag: make worktrees the undeniable default | No new context this session. |
| unitAI-tz4r | P1 | Integrate OpenWolf-style memory protocol into specialist lifecycle | No new context this session. |
| unitAI-klz1 | P2 | specialist:debugger | No new context this session. |
| unitAI-xet7 | P2 | Uniform output contract: slim output_schema across all specialists | No new context this session. |
| unitAI-agvk | P2 | Replace symlink-based skill sharing with npm package resolution | `specialists init --sync-skills` requires interactive terminal — cannot run from scripts or agents. This blocked skill drift recovery this session. |
| unitAI-231x | P2 | feat: default context-depth to 3 | No new context this session. |
| unitAI-bt1f | P2 | feat: --job auto-bead resolution from job metadata | No new context this session. |
| unitAI-tiah | P2 | Infra/DX: process management, tmux, init guards, bug fixes | `sp merge` silently succeeded but didn't update master HEAD — the merge ran in-process but the working tree wasn't updated. Worth investigating `sp merge` reliability. |
| unitAI-brbb | P3 | fix: pass explicit git root cwd to createObservabilitySqliteClient() | No new context. |
| unitAI-48pb | P3 | feat: specialist memory system | No new context. |

## Memories Saved

| Key | Content |
|-----|---------|
| `sp-node-steer-node-id-message-steers-the` | `sp node steer <node-id> <message>` steers the coordinator (writes to coordinator FIFO), not a member. The name implies member targeting but it doesn't. Future member-targeted steering needs a new command or use `sp steer <job_id>` directly with job_id from status --json. |

## Suggested Next Priority (Session 1)

1. **`unitAI-96qy` — E2E node coordination test** — The coordinator now has the full tool surface (result, steer, resume). The only way to know if it actually uses it correctly is to run a real node and watch. This validates everything shipped in `unitAI-8zui` end-to-end.
2. **`unitAI-2b3m` — Research Node architecture** — Now that the coordinator skill is complete, running `research.node.json` with a real task is the natural next step. Confirm the coordinator phases correctly: spawns explorer, reads result, steers researcher with specific findings, synthesizes.
3. **`unitAI-ywsr` — Sync docs fresh pass** — The concurrent sync-docs job left some drift. A targeted sync on node docs (`docs/nodes.md`, `docs/cli-reference.md`) would pick up `sp node result` and the new coordinator behaviors.
4. **`sp merge` reliability investigation** — Three times this session `sp merge` reported success but the branch wasn't on master. Either the command has a bug or it's working in a different context than expected. File as a bug if not already tracked.

---

# Session 2 — 2026-04-11 (continued)

## Summary

Systematic bug-fix sprint targeting observability and epic infrastructure reliability. Fixed 5 bugs that made the epic system and `sp ps`/`sp stop`/`sp merge` unusable for daily operations: (1) epic chain membership not syncing on job completion — added `syncEpicOnJobComplete` helper to both terminal paths in supervisor.ts; (2) zombie waiting jobs persisting after process death — enhanced `crashRecovery()` with run_complete evidence checking, `hasRunCompleteEvent()` with SQLite+file fallback, and 6-hour `waiting_timeout_ms`; (3) `sp ps --follow` rendering completely broken — dedupe via `dedupeStatusesById` + `renderedJobIds` Set, bold status colors, dim metadata, cyan epic banner, sanitized bead titles, stable footer from unique rendered jobs; (4) `sp stop` leaving jobs in zombie waiting state — now writes terminal status (`done`/`cancelled`) to status.json before SIGTERM, introducing `cancelled` as a new `SupervisorJobStatus` with full type propagation through timeline events and ps rendering; (5) `sp merge` file listing wrong + blocking doc-only merges — fixed file listing to use `git diff HEAD^1 HEAD`, then replaced the hardcoded `src/` validation with a merge-preview worthiness guard (overthinker-designed) that inspects the actual staged delta and classifies files as substantive vs noise. Also discovered and documented: reviewer blind spot (validates executor claims against pre-existing code without diffing branch vs master), codex empty-output pattern (gpt-5.3-codex intermittently returns 0 tokens on turn 1), and worktree branch divergence (parallel executors need rebase before merge when waves are sequential).

## Issues Closed (14 this session)

### Bugs Fixed

| ID | Title | Specialist | Wave | Close Reason |
|----|-------|-----------|------|--------------|
| unitAI-vozx | Epic chain membership not synced on job completion | explorer 296b5e → executor 132938 → reviewer 9878ce | W1→W2→W3 | Merged: `syncEpicOnJobComplete` helper in both success+error paths. Reviewer PASS. |
| unitAI-ug51 | Zombie waiting jobs in observability DB after process exit | explorer f23d3f → debugger 48211f → reviewer 104875 | W1→W2→W3 | Merged: crashRecovery reconciliation + hasRunCompleteEvent + 6h waiting timeout. Reviewer PASS 93/100. |
| unitAI-fh7o | sp ps --follow rendering: dedupe, visual hierarchy, epic banner | executor 580e3d | W4 | Merged: all 6 rendering items. First attempt (xuvg) was ghost executor — code never landed. |
| unitAI-y4ia | sp stop must write terminal status before SIGTERM | executor 12a519 (codex) + manual type fixes | W5 | Merged: `cancelled` status, terminal write before SIGTERM. Type propagation across 5 files. |
| unitAI-tzzu | sp merge validate branch diff + fix file listing | executor 1196da | W5 | Merged: `assertBranchHasSourceDiff` + `readChangedFilesForLastMerge`. Later replaced by wisz. |
| unitAI-wisz | Merge-preview worthiness guard replacing src/ hardcode | overthinker 96c7cb → executor c3aeb9 | W6→W7 | Merged: `previewBranchMergeDelta` + `evaluateMergeWorthiness` with noise classification. |

### Exploration/Design (consumed by impl beads)

| ID | Title | Specialist | Close Reason |
|----|-------|-----------|--------------|
| unitAI-euxx | Explore: map epic chain membership sync | explorer 296b5e | Findings consumed by ntjv |
| unitAI-fpk8 | Explore: map zombie waiting job lifecycle | explorer f23d3f | Findings consumed by 9mq2 |
| unitAI-ntjv | Implement: sync epic chain membership | executor 132938 | Merged via vozx chain |
| unitAI-9mq2 | Implement: reconcile zombie waiting jobs | debugger 48211f | Merged via ug51 chain |
| unitAI-jkx4 | Design: sp merge empty-branch guard | overthinker 96c7cb | Design consumed by wisz |

### Doc Sync

| ID | Title | Specialist | Close Reason |
|----|-------|-----------|--------------|
| unitAI-ywsr | Sync docs: all session fixes | sync-docs 7f0940 | Merged: CLAUDE.md, cli-reference, features, changelog |
| unitAI-8es5 | Sync docs: merge guard | sync-docs b60ed8 | Merged: cli-reference, features |

### Previously Closed (confirmed)

| ID | Title | Status |
|----|-------|--------|
| unitAI-m749 | sp ps --follow flicker | Was already closed — original flicker fix landed, but extended scope (6 items) was not done. Fixed via fh7o. |
| unitAI-lzys | Wave management epic | Already closed with child. |

## Issues Filed (12 this session)

| ID | P | Type | Title | Why |
|----|---|------|-------|-----|
| unitAI-euxx | P1 | task | Explore: map epic chain membership sync | Needed codebase mapping before surgical fix — exact line numbers for completion paths. |
| unitAI-fpk8 | P0 | task | Explore: map zombie waiting job lifecycle | Complex multi-cause bug — needed full lifecycle trace before implementation. |
| unitAI-ntjv | P1 | task | Implement: sync epic chain membership | Implementation tracking for vozx fix. |
| unitAI-9mq2 | P0 | task | Implement: reconcile zombie waiting jobs | Implementation tracking for ug51 fix. |
| unitAI-xuvg | P2 | bug | Fix sp ps --follow rendering (first attempt) | Ghost executor — closed prematurely, code never landed. Superseded by fh7o. |
| unitAI-fh7o | P2 | bug | Fix sp ps --follow rendering (redo) | Second attempt after discovering xuvg merge was empty. |
| unitAI-y4ia | P1 | bug | sp stop must write terminal status before SIGTERM | Discovered live: every executor we stopped stayed in 'waiting'. |
| unitAI-tzzu | P2 | bug | sp merge validate diff + fix file listing | Two separate merge issues found during session. |
| unitAI-jkx4 | P1 | decision | Design: merge empty-branch guard | Overthinker needed — src/ hardcode was wrong approach, needed architectural rethink. |
| unitAI-wisz | P1 | task | Implement: merge-preview worthiness guard | Implementation of overthinker design. |
| unitAI-ywsr | P2 | task | Sync docs: all fixes | 7 doc targets needed updating after 5 code fixes. |
| unitAI-8es5 | P2 | task | Sync docs: merge guard | Targeted sync after guard redesign. |

## Specialist Dispatches (18 this session)

| Wave | Job | Specialist | Model | Bead | Elapsed | Outcome |
|------|-----|-----------|-------|------|---------|---------|
| W1 | 296b5e | explorer | qwen3.5-plus | euxx | 2m33s | DONE — mapped spawn vs completion gap in supervisor.ts |
| W1 | f23d3f | explorer | qwen3.5-plus | fpk8 | 2m40s | DONE — traced 4 locations where 'waiting' is set, identified run_complete ≠ done |
| W2 | 132938 | executor | gpt-5.3-codex | ntjv | 1m46s | DONE — syncEpicOnJobComplete helper, both paths |
| W2 | 48211f | debugger | gpt-5.3-codex | 9mq2 | 2m52s | DONE — crashRecovery enhancement + hasRunCompleteEvent + waiting_timeout_ms |
| W3 | 9878ce | reviewer | gpt-5.3-codex | — | 1m03s | PASS 84 — vozx code correct, gaps are test coverage only |
| W3 | 104875 | reviewer | gpt-5.3-codex | — | 1m37s | PASS 93 — all 5 requirements met |
| W4a | 33ddbb | executor | gpt-5.3-codex | xuvg | 2m42s | GHOST — claimed success, wrote 0 code. result.txt convincing but branch empty. |
| W4a | a79781 | reviewer | gpt-5.3-codex | — | 1m12s | INVALID — reviewed ghost executor's claims against pre-existing code |
| W4b | 090ece | executor | gpt-5.3-codex | xuvg | 5m19s | DONE — 140+/65- in ps.ts, real code. Reviewer reviewed wrong job (33ddbb). |
| W4b | 4224b4 | reviewer | gpt-5.3-codex | — | 1m02s | PARTIAL 72 — reviewed wrong job (33ddbb not 090ece) |
| W4c | 580e3d | executor | gpt-5.3-codex | fh7o | 3m31s | DONE — all 6 ps rendering items, 69+/34- |
| W5 | 12a519 | executor | gpt-5.3-codex | y4ia | 4m12s | ERROR — wrote code (75 insertions) then crashed. Manual type fixes needed. |
| W5 | 1196da | executor | gpt-5.3-codex | tzzu | 2m18s | DONE — merge validation + file listing fix |
| W6 | 96c7cb | overthinker | gpt-5.4 | jkx4 | 2m49s | DONE — 4-phase design: merge-preview worthiness guard |
| W7 | c3aeb9 | executor | gpt-5.3-codex | wisz | 2m14s | DONE — 102+/23- implementing overthinker design |
| W8 | 7f0940 | sync-docs | glm-5 | ywsr | 7m51s | DONE — but also modified source files (node.ts). Had to cherry-pick docs only. |
| W9 | b60ed8 | sync-docs | glm-5 | 8es5 | 6m22s | DONE — targeted merge guard docs |
| — | a93094, 7aa841, 61d379, bd9c4e | executor | gpt-5.3-codex | y4ia/tzzu | ~17s each | FAILED — 0 tokens, 0 tools. Codex empty-output pattern. |

### Problems

| Problem | Root Cause | Resolution |
|---------|-----------|------------|
| Ghost executor (33ddbb) claimed success, wrote nothing | Codex returned convincing result.txt but never called edit tools | Re-dispatched, verified diff before sending to reviewer |
| Reviewer (a79781, 4224b4) validated phantom code | Reviewer checks executor claims against codebase, doesn't diff branch vs master | Saved as memory: always verify branch diff before dispatching reviewer |
| Codex empty output (4× on y4ia/tzzu) | gpt-5.3-codex intermittently returns 0 tokens on turn 1 | Kill + retry; eventually worked. Parallel dispatch may increase failure rate. |
| Executor 12a519 wrote code then crashed | Backend error after code was written but before completion | Manual type fixes for `cancelled` status propagation (ps.ts, timeline-events.ts) |
| sp merge refused sync-docs branch | `assertBranchHasSourceDiff` hardcoded `src/` — blocked doc-only merges | Manual `git merge`, then redesigned guard via overthinker |
| Worktree branch divergence (tzzu) | Worktree branched before y4ia merge, diff showed revisions of stop fix | `git rebase master` in worktree before merging |
| sync-docs (7f0940) modified source files | sync-docs specialist exceeded scope — added node.ts features | Cherry-picked docs only, discarded source changes |
| xuvg merge reported `files: (none)` | `readChangedFilesForHead` used `git diff-tree` which misreported | Fixed with `git diff HEAD^1 HEAD` in tzzu |
| sp stop left jobs in 'waiting' (hit 3×) | SIGTERM kills process but doesn't update status.json | Fixed in y4ia: writes terminal status before SIGTERM |

## Code Changes

### supervisor.ts — Epic sync + zombie reconciliation
- `syncEpicOnJobComplete()` — new helper calling `upsertEpicChainMembership` + `loadEpicReadinessSummary` + `syncEpicStateFromReadiness` on both success and error completion paths
- Enhanced `crashRecovery()` — dead PID + run_complete → done, dead + no evidence + non-node → error, node members preserved
- `waiting_timeout_ms` in `STALL_DETECTION_DEFAULTS` (6h default)
- `SupervisorJobStatus` type now includes `'cancelled'`
- `updateJobStatus()` method for external status writes

### observability-sqlite.ts — run_complete detection
- `hasRunCompleteEvent(jobId)` — SQLite-first query with events.jsonl fallback

### stop.ts — Terminal status before SIGTERM
- `resolveTerminalStatus()` checks run_complete evidence → `done` or `cancelled`
- Writes status.json before sending SIGTERM

### ps.ts — Visual hierarchy overhaul
- `dedupeStatusesById()` + `renderedJobIds` Set preventing duplicate rows
- `sanitizeBeadTitle()` — whitespace collapse preventing line bleed
- Status colors: running=bold green, waiting=bold magenta, error=bold red, done/cancelled=dim
- Dimmed metadata (elapsed, tokens, context)
- Epic banner: bold cyan `┏━ EPIC` header
- Footer computed from unique rendered jobs

### merge.ts — Worthiness guard
- `previewBranchMergeDelta()` — `git merge --no-commit`, inspects `git diff --cached --name-status`, aborts in finally
- `evaluateMergeWorthiness()` — empty delta → block, noise-only → block, substantive → allow
- `NOISE_PATH_PREFIXES` — `.xtrm/reports/`, `.wolf/`, `.specialists/jobs/`
- `readChangedFilesForLastMerge()` — `git diff HEAD^1 HEAD` for accurate file listing
- Removed: `assertBranchHasSourceDiff`, `readBranchSourceChangesAgainstMaster`, `readBranchDiffStatAgainstMaster`

### timeline-events.ts — cancelled status
- `TimelineEventStatusChange` status/previous_status unions include `'cancelled'`

### schema.ts + loader.ts — waiting timeout config
- `waiting_timeout_ms` added to stall detection config

## Documentation Updates

- **CLAUDE.md** — Job status lifecycle section, crash recovery section, `cancelled` status, stop behavior, merge validation, epic chain auto-sync
- **docs/cli-reference.md** — sp stop behavior section, sp merge worthiness guard, noise classification, cancelled in ps --all, merge flow steps updated
- **docs/features.md** — New sections: Job status lifecycle (#7), Crash recovery (#22), Epic chain membership auto-sync (#23). Updated stuck detection, sp merge docs.
- **CHANGELOG.md** — All 7 changes under [Unreleased]

## Memories Saved

| Key | Content |
|-----|---------|
| `specialist-jobs-using-interactive-keep-alive-mode-executor` | SIGTERM'd keep-alive jobs stay 'waiting' — mid-session workaround is manual status.json patch. ug51 fix handles it on next startup. |
| `reviewer-blind-spot-reviewer-validates-executor-claims-again` | Reviewer doesn't diff branch vs master — validates executor claims against pre-existing code. Always verify branch diff before dispatching reviewer. |
| `codex-executor-empty-output-pattern-gpt-5-3` | gpt-5.3-codex intermittently returns 0 tokens on turn 1. Kill and retry. Parallel dispatch may increase failure rate. |
| `sp-merge-worktree-branch-divergence-when-parallel-executors` | Sequential wave worktrees need rebase before merge — earlier wave's changes appear as reversions. |
| `sp-merge-src-validation-guard-is-too-strict` | Superseded by wisz implementation — merge-preview worthiness guard now replaces src/ hardcode. |

## Open Issues with Context

### Ready for next session

| ID | P | Title | Context / Suggestions |
|----|---|-------|----------------------|
| unitAI-aq1k | P1 | Eliminate --no-worktree flag | Strengthens isolation model. No new blockers. Ready to implement. |
| unitAI-t4ss | P1 | Coordinator CLI-native redesign (epic) | Already in progress in another worktree (1z00, lpus jobs). Check `sp ps --all` for status. |

### Backlog

| ID | P | Title | Context / Suggestions |
|----|---|-------|----------------------|
| unitAI-96qy | P1 | E2E node coordination runtime test | Coordinator now has full tool surface from session 1. Test validates 8zui + this session's fixes. |
| unitAI-tiah | P2 | Infra/DX: process management, tmux, init guards | sp merge reliability now fixed (file listing + worthiness guard). May be partially resolved. |

## Suggested Next Priority (Session 2)

1. **`unitAI-aq1k` — Eliminate --no-worktree** — User stated this as next after the bug sprint. Strengthens isolation model.
2. **`unitAI-t4ss` — Coordinator CLI-native redesign** — Already in progress, check worktree status.
3. **`unitAI-96qy` — E2E node coordination test** — Validates both sessions' work end-to-end.
