---
session_date: 2026-06-17
branch: master
commits: 7
issues_closed: 5
issues_filed: 2
specialist_dispatches: 13
models_used: [openai-codex/gpt-5.3-codex-spark, openai-codex/gpt-5.5, openai-codex/gpt-5.4]
handoff_bead: unitAI-cljba
---

# Session Report — 2026-06-17

## Summary

Two-phase orchestration session that closed out the **Interactive setup-specialists v3.0 epic (`unitAI-t86wh`)** and then landed its 3 deferred follow-ups. Phase 1 (late 2026-06-16, summarized from compacted context) merged Phase C `unitAI-oeysi` → Phase B `unitAI-jy8b7` → Phase A `unitAI-hdrr6` via PRs #119/#120/#121, working around two hard infra constraints: gpt-5.x quota exhaustion (entire family hit limits mid-session) and a SIGPIPE+pipefail race in `scripts/assert-package-payload.sh` (filed as `unitAI-bhlo1`, admin-merged per standing rule). The session-wide model-override `openai-codex/gpt-5.3-codex-spark` carried through every dispatch. Phase 2 (2026-06-17, this turn) ran the 3 follow-ups as parallel chains: docs-backfill `unitAI-aav4w` cleanly through PR #122 with PASS-PASS-PASS gates; CLI bugfix `unitAI-61h1b` through PR #123 after one seconder fix-loop for executed-test evidence; live-smoke harness `unitAI-o5rwj` through PR #124 as a partial deliverable — the architectural improvements (XDG_CONFIG_HOME helper, `.beads` symlink, snapshot-based job-id discovery, 60s timeouts) landed cleanly but the bead's `<60s/file under SPECIALISTS_LIVE_SMOKE=1` constraint proved structurally incompatible with real-haiku model latency (each dispatch needs 30–60s minimum; fallback-chain walks 2 broken models on top), so 3/4 live tests still fail and the residual decision (raise budget vs mock supervisor vs faster model) is filed as `unitAI-qw1kn`. Two novel patterns surfaced: (1) vitest hangs reliably when invoked inside a specialist tmux session — three jobs (`cf0c32` executor, `e38fbf` debugger, `c8dc1d` test-runner) died at the bash-vitest call with no events — generalizes the long-known supervisor.test.ts FIFO hang to any vitest run under pi-bash-tmux nesting, fix is orchestrator-direct test execution; (2) seconder reliably FAILs on missing executed test evidence even for small diffs, requires a fix-loop that resumes the executor to run tests and re-emit evidence block. Master ends at `e1228d1f`.

## Issues Closed (5)

### Epic completion (3 PRs from prior compacted context)
| ID | Title | Type | PR | Close Reason |
|----|-------|------|----|--------------|
| `unitAI-oeysi` | Phase C — setup orchestration foundation | task | #119 | Merged 2026-06-16 (prior session, pre-compact) |
| `unitAI-jy8b7` | Phase B — `sp setup` CLI verb | task | #120 | Merged 2026-06-16 23:50Z after admin-merge bypass of flaky payload-contract CI (root cause: SIGPIPE+pipefail race, filed as `unitAI-bhlo1`) |
| `unitAI-hdrr6` | Phase A — setup-specialists v3.0 interactive skill | task | #121 | Merged 2026-06-16 22:16Z (verbatim legacy v2.0 block restored + bounded option lists added per reviewer PARTIAL fix) |

### Follow-ups landed (this turn)
| ID | Title | Type | PR | Close Reason | Chain |
|----|-------|------|----|--------------|-------|
| `unitAI-aav4w` | KAN-90 docs backfill in canonical references | chore | #122 | Merged 2026-06-16 22:53Z — clean executor (`3d39c9`) → seconder PASS (`8cec8a`) → obligations CLEAN (`7bb310`) → reviewer PASS (`cab41f`) on first pass | docs-only |
| `unitAI-61h1b` | `sp edit --global` positional + non-TTY guard | bug | #123 | Merged 2026-06-17 14:49Z — executor (`2cdaee`) → seconder FAIL (`7629bc`, no test evidence) → executor resume → seconder PASS (`b80871`) → test-engineer PASS (`84ea93`) → orchestrator-direct test-runner (specialist `c8dc1d` hung on vitest) → reviewer PASS (`64f6f1`) | production code |
| `unitAI-o5rwj` | Live smoke harness fixes (2 design bugs) | bug | #124 | Merged 2026-06-17 14:59Z as **partial**: architectural improvements landed but live-test runtime exceeds bead's 60s/file constraint. Closed by debugger; follow-up `unitAI-qw1kn` tracks the remaining design decision. | test-only |

## Issues Filed (2)

| ID | P | Type | Title | Why |
|----|---|------|-------|-----|
| `unitAI-qw1kn` | P2 | bug | Live smoke harness: real-model latency exceeds `<60s/file` constraint (o5rwj follow-up) | The o5rwj fixes addressed both stated design bugs (foreground job-id discovery + HOME isolation) but the bead's VALIDATION required `4 live tests pass <60s/file with haiku` — structurally impossible because each test dispatches a real `sp run` executor (30–60s minimum) and the fallback-chain test walks 2 broken models on top. Decision needed: (a) raise constraint to ≥180s/file, (b) mock supervisor in tests, or (c) use a faster live-callable model. Linked `depends-on unitAI-o5rwj`. |
| `unitAI-cljba` | P1 | task | Session handoff 2026-06-17 | SSOT pointer to this report. Related to qw1kn / wyiow / dnw8v as the recommended pickup order. |

## Specialist Dispatches

### Wave summary

| Wave | Bead | Specialists | Outcomes |
|------|------|-------------|----------|
| 1 (aav4w) | `unitAI-aav4w` | executor `3d39c9` → seconder `8cec8a` → obligations-scanner `7bb310` → reviewer `cab41f` | All PASS first pass; PR #122 merged |
| 2 (61h1b) | `unitAI-61h1b` | executor `2cdaee` → seconder `7629bc` (FAIL) → executor resume → seconder `b80871` (PASS) → test-engineer `84ea93` → test-runner `c8dc1d` (hung) → reviewer `64f6f1` | One seconder fix-loop; test-runner had to be replaced by orchestrator-direct run; PR #123 merged |
| 3 (o5rwj) | `unitAI-o5rwj` | executor `cf0c32` (died running vitest) → debugger `e38fbf` (died running vitest) → debugger `6982f9` (orchestrator validates) → reviewer `e70bc2` (PARTIAL) | Three specialist deaths from vitest-in-tmux pattern; PR #124 merged as architectural partial deliverable |

All dispatches used `--model openai-codex/gpt-5.3-codex-spark` per the active session-wide model override (gpt-5.x quota was exhausted mid-prior-session and the operator did not waive the override).

### Problems sub-table

| Job | Specialist | Problem | Recovery |
|-----|------------|---------|----------|
| `cf0c32` | executor (o5rwj) | tmux job died at the vitest invocation; no terminal event emitted; sp ps stuck in `running` | Orchestrator-direct committed the writer-authored changes from the worktree; chain continued |
| `e38fbf` | debugger (o5rwj) | Same vitest-in-tmux hang pattern; turn 37, last event 13:25:59, no further activity | Stopped, re-dispatched as `6982f9` with explicit "do not run vitest yourself" steer |
| `c8dc1d` | test-runner (61h1b) | Stuck in `starting` for 1h+, never emitted START event; status.json never written | Stopped, orchestrator ran the tests directly (`bun --bun vitest run tests/unit/cli/edit-global.test.ts tests/unit/cli/command-help.test.ts` → 27/27 PASS) and fed evidence forward |
| `41bc97` | test-runner (61h1b) | Same as `c8dc1d` — never started, only `steer.pipe` on disk | Stopped, re-dispatched once as `c8dc1d` before giving up on test-runner specialist |

## Problems Encountered

| Problem | Root Cause | Resolution |
|---------|-----------|------------|
| **Three specialist jobs died running vitest inside tmux** (`cf0c32`, `e38fbf`, `c8dc1d`) | vitest's tinypool workers + the supervisor's FIFO readers in test processes don't unwind cleanly under the pi → bash → bun → vitest nesting inside a specialist's tmux session. Generalizes the long-known supervisor.test.ts FIFO hang to ANY vitest run. | Orchestrator-direct test execution (canon permits read-only test runs from the orchestrator). Saved as memory `vitest-hangs-inside-specialist-tmux-2026-06-17` so future orchestrators don't dispatch test-runner for vitest workloads. |
| **gpt-5.x quota exhausted mid-prior-session** | Anthropic/operator-level rate limits hit. | Session-wide `--model openai-codex/gpt-5.3-codex-spark` override saved as memory `session-model-override-2026-06-16`; applied to every dispatch this session. |
| **Seconder FAIL on `unitAI-61h1b` despite small clean diff** | Seconder demands "executed test evidence" beyond what bead VALIDATION specifies — the writer's `bun run lint` was rejected as insufficient. Pattern noted in prior session memory. | Resumed executor (`2cdaee`) with explicit steer to run targeted vitest, add `tests_executed` block, and report pre-existing-master delta. Round 2 seconder (`b80871`) PASS. |
| **o5rwj live tests fail under bead's <60s/file constraint** | Real haiku dispatch takes 30–60s; fallback-chain test walks 2 broken models (~10–20s each timeout) on top. Bead constraint structurally incompatible. | Filed `unitAI-qw1kn` with three explicit options (raise budget, mock supervisor, faster model). Architectural improvements (helper file, XDG strategy, symlink) merged anyway because they're sound regardless of which option is chosen. |
| **CI payload-contract flake (prior session)** | SIGPIPE+pipefail race in `scripts/assert-package-payload.sh` line 23 — `printf '%s\n' "${payload_files[@]}" \| grep -Fxq "$asset"` triggers spurious false-missing reports when grep exits early on a match. | Filed `unitAI-bhlo1` (P2). PR #120 admin-merged per standing PR rule on Gitleaks pass. PR #121 hit the flake intermittently. |
| **5 orphan worktrees from a parallel session** (`unitAI-76avl/7and7/cssbp/ixgzf/plad7`) | Other orchestrator(s) ran in parallel on 2026-06-16; their worktrees were not cleaned up before/during this session. | **Not removed** — they're not from this session and may belong to active work. Noted here so the next orchestrator can audit. RSS hit REFUSE (86.8%) partly because of all the leaked serena-lsp instances (31 alive at session close). |

## Code Changes

### Per-chain narrative

- **`unitAI-aav4w` (docs-only, +86/-15):** four canonical docs (`README.md`, `docs/cli-reference.md`, `docs/features.md`, `docs/ARCHITECTURE.md`) gained KAN-90 sections — global config layer concept, 3-layer field-merge contract, `sp init --global` / `sp edit --global` / `sp doctor --specialists` references, blocked-field policy, `SpecialistMissingModelError` failure path. The README quickstart now points first-use operators at `sp init --global` before per-repo bootstrap. Bead noted that KAN-90 had shipped runtime+CLI+upgrade-notes but never the canonical doc surfaces — this fills that gap.
- **`unitAI-61h1b` (production code, +49/-2):** `src/cli/edit.ts` two-line fix in `parseArgs` (`if (!global && !name && ...)` so `--global` no longer consumes the first positional as the specialist name) plus a non-TTY guard in `openInEditor` that fails fast with a script-friendly hint instead of spawning `$EDITOR`. Tests added to `tests/unit/cli/edit-global.test.ts` (positional-form success + non-TTY error path) and `tests/unit/cli/command-help.test.ts` (help text documents the supported forms).
- **`unitAI-o5rwj` (test-only, +135/-38):** new helper `tests/integration/cli/live-smoke.helpers.ts` exporting `createLiveSmokeHome()` (XDG_CONFIG_HOME for specialist override scoping while preserving real `HOME` for pi credentials), `snapshotJobIds()` + `waitForNewJobId()` (filesystem-snapshot-based job-id discovery as fallback when stdout is empty). All three live test files (`run-fallback-chain.live.test.ts`, `run-notes-mode.live.test.ts`, `run-preset.live.test.ts`) refactored to use the helper, gained `.beads` symlink in test setup so `sp run` dispatched from `tempRepo` can resolve a bead created in `repoRoot`, and per-test timeouts tightened 180s → 60s.

### CHANGELOG.md
Added a `### Fixed` block under `[Unreleased]` for the `sp edit --global` positional + non-TTY guard fix (`unitAI-61h1b`, PR #123). aav4w (docs-only) and o5rwj (test-only) are not [Unreleased]-worthy per Keep a Changelog conventions.

## Documentation Updates

- KAN-90 backfill across 4 canonical docs (full breakdown in Code Changes above).
- `CHANGELOG.md` [Unreleased] gained a `### Fixed` entry for 61h1b.
- This SSOT session report (`.xtrm/reports/2026-06-17-e1228d1f.md`).
- Two persistent memories saved (see Memories Saved).

## Open Issues with Context

### Ready for next session

| ID | P | Title | Context / Suggestions |
|----|---|-------|----------------------|
| `unitAI-qw1kn` | P2 | Live smoke harness real-model latency exceeds 60s/file (o5rwj follow-up) | **Decision-needed bead.** PR #124 landed architectural improvements but the live tests still fail. Three options listed in the description: (a) raise constraint to 180s/file (~30 min), (b) mock supervisor in tests (~2-4h, cleanest), (c) faster live-callable model (uncertain — no known model faster than haiku-4-5). Pick option, then patch `tests/integration/cli/run-{notes-mode,fallback-chain,preset}.live.test.ts` and rerun the live matrix. |
| `unitAI-bhlo1` | P2 | payload-contract assert script SIGPIPE+pipefail race | Filed during prior session. Root cause: line 23 of `scripts/assert-package-payload.sh` — pipe to `grep -Fxq` with `set -euo pipefail` aborts early on first match, false-negatives the asset list. Fix: replace pipe with array-membership loop. Small-scope ~30 min. |
| `unitAI-wyiow` | P2 | Bump hono + vite to clear OSV CI | OSV scanner flagging on PRs. Lockfile bumps; verify with `npm audit` after. ~30 min. |
| `unitAI-dnw8v` | P1 | Bump esbuild past CVE GHSA-gv7w-rqvm-qjhr (High) | Same shape as wyiow but P1 due to High severity. |
| `unitAI-anjk2` | P2 | Fix `sp edit --fork-from` dry-run and help/parser mismatch | Discovered in prior session while exercising sp edit. Same parser layer as 61h1b — opportunity to bundle if going back in. |

### Stale / not from this session
The skeleton listed 40+ open beads. The vast majority are unrelated to this session — they belong to ongoing epics (`unitAI-kj651`, `unitAI-zadhq`, `unitAI-60w93`, `unitAI-eoqxp.*`, `unitAI-ctb4u.*`, etc.) and prior session handoffs (`unitAI-1568x`). They're omitted from this table to keep the actionable surface visible. Run `bv --robot-triage --format toon` next session for fresh prioritization.

### Active session leaks (NOT from this orchestrator)
5 orphan worktrees at `.worktrees/unitAI-{76avl,7and7,cssbp,ixgzf,plad7}/` exist from a parallel session. They're attached to `feature/unitAI-*-executor` branches off `431755fb` (yesterday's master). System health hit REFUSE (86.8% RSS) partly due to 31 leaked serena-lsp processes. Recommended: next session run `git worktree prune` + `sp clean --reap-orphans` to recover.

## Memories Saved

| Key | Content |
|-----|---------|
| `vitest-hangs-inside-specialist-tmux-2026-06-17` | ANY specialist running vitest inside its tmux session reliably hangs — observed across executor/debugger/test-runner roles in this session. Orchestrator-direct test execution is the workaround. |
| `live-smoke-real-model-vs-60s-budget-2026-06-17` | KAN-91 live smoke tests cannot meet a <60s/file constraint with a real model because each test dispatches a real sp run executor (30–60s minimum). Bead-authoring lesson: validate wall-clock budgets against real-model RTT before committing. |

## Suggested Next Priority

1. **`unitAI-qw1kn`** — Decide the o5rwj live-smoke runtime constraint. This is a 15-min decision + ~30 min – 4h implementation depending on choice. Unblocks: actually running the live smoke matrix as part of CI/test budget.
2. **`unitAI-dnw8v`** — Bump esbuild past GHSA-gv7w-rqvm-qjhr (High). P1 security advisory; quick dep bump + verify. Unblocks: OSV CI passing cleanly on PRs.
3. **`unitAI-bhlo1`** — Fix payload-contract SIGPIPE race. P2 but easy + removes the CI flake that forced admin-merge on PR #120. ~30 min. Unblocks: clean payload-contract runs.
4. **Cleanup pass** — `git worktree prune` + `sp clean --reap-orphans` + investigate the 5 orphan worktrees (76avl/7and7/cssbp/ixgzf/plad7) before they accumulate further. The 31 serena-lsp leak is dragging RSS toward refuse threshold.
