---
session_date: 2026-04-26
branch: master
commits: 17
issues_closed: 22
issues_filed: 14
specialist_dispatches: 7
models_used:
  - openai-codex/gpt-5.4-mini
  - openai-codex/gpt-5.3-codex
  - nano-gpt/moonshotai/kimi-k2.5
---

# Session Report — 2026-04-26 (specialists-service hardening + schema strict-cut)

> Note: a separate parallel effort (fln4q-epic, ~10 merge commits) shipped the
> SQLite observability migration in the same calendar window. **This report
> covers only the specialists-service / schema-discipline track**; do not read
> the fln4q-* commits as part of this thread.

## Summary

Continued the specialists-service v1 thread from the prior 2026-04-26 morning report (`523fc559`). This session moved through five connected layers: (1) finished the post-ship safety pass — Zod `.passthrough()` so `sp edit` round-trip stops dropping keys (`unitAI-xutg2`), pi 0.70.x compat by removing the `--` option terminator and unpinning the image to `@latest` (`unitAI-w0h7z`), and a CI canary at `.github/workflows/pi-compat.yml` that fails loud on any future spawn-flag drift (`unitAI-nsru6`); (2) built the rest of the darth-feedor adoption surface — `sp script` cron-friendly CLI peer (`unitAI-6qctn`), `sp validate <path> --target script` for offline pre-deploy checks (`unitAI-bahj1`), reference Python adapter at `docs/examples/specialists_client.py` (`unitAI-s2won`, live-smoked end-to-end), `mercury-atomic-summarizer` PoC spec (`unitAI-t9t11`, also live-smoked), `--user-dir` → `--project-dir` rename with deprecated alias (`unitAI-rfjbd`), surfaced both new commands in `sp --help`, and tagged the canonical release `v3.8.0`; (3) discovered a JSON corruption in `config/specialists/specialists-creator.specialist.json:18` (a zsh prompt artifact had been pasted into the spec — `"permission_requiredspecialists — zsh "` instead of `"permission_required"`) that no audit had caught because parse-with-passthrough silently kept it (`unitAI-826wl`); (4) ran three parallel explorer audits (`35c107`, `60f59b`, `0ec693`) and discovered the explorers had only sampled 3 of 26 specs — full sweep found `xt-merge.communication.output_to` was a typo'd dead alias hiding the silent drop of `specialist.output_file` (xt-merge had been failing to write its result file since the spec was authored), plus 19 stale `.xtrm/skills/active/pi/<name>/` skill paths across the canonical and mirror specs (`unitAI-yb9qu`, then mirror sync); (5) executed user direction "drop everything in JSON not read by the runner — source of truth, no nice-to-have leftovers, 1:1 with reality": dropped 11 schema declarations (`CommunicationSchema` entirely, `next_specialists`, `publishes`, `diagnostic_scripts`, `normalize_template`, `examples`, `preferred_profile`, `approval_mode`, `metadata.author`, `metadata.created`, `heartbeat`, `ScriptEntry.path`), stripped them from 26 specs, updated `docs/authoring.md` + `config/skills/specialists-creator/SKILL.md` + `view.ts` + `scaffold-specialist.ts` in lockstep, and committed a portable audit script at `config/skills/specialists-creator/scripts/audit-spec-uniformity.mjs` so this drift detection is reproducible (`unitAI-8n0aa`). Schema is now strictly 1:1 with the runner. Closed the loop with `docs/darth-feedor-migration.md` + Python adapter alignment (Phase 3 analyst now ships **two** specialist files instead of one with two templates, since alternate-template lookup never worked) and a one-page `handoff-feedor.md` at the repo root pointing at every artifact darth-feedor needs to copy. Observability check (separate question raised mid-session): GitNexus and Serena tool families are both actively used — 180 GitNexus calls and 110 Serena calls across 5 specialists in the prior 16 days — but specialists bypass the `~/.pi/settings.json` `serena.blockedTools` enforcement that interactive pi enforces, because `src/pi/session.ts:540` explicitly passes `--tools read,bash,edit,write,grep,find,ls` based on `permission_required`. This is by design (permission_required is the canonical authority for specialists) but means the doc-prescribed Serena-first workflow is aspirational in specialist runs — flagged but not fixed pending operator decision.

## Issues Closed (22)

Grouped by track. Specialist runs are in the Specialist column where applicable.

### Adoption surface — sp serve hardening + sp script CLI

| ID | Title | Specialist | Wave | Close Reason |
|---|---|---|---|---|
| `unitAI-xutg2` | Zod schema passthrough — preserve unknown keys on parse | executor `3cfa97` (gpt-5.4-mini) | impl | Added `.passthrough()` to every nested object in SpecialistSchema; new `tests/unit/specialist/schema-passthrough.test.ts` asserts root + nested unknown keys survive. Executor died mid-vitest; orchestrator ran gates manually (test passes, 13 unrelated pre-existing failures unchanged) and merged. |
| `unitAI-bx72n` | Review Zod schema passthrough fix | (manual review by orchestrator) | review | PASS — diff was 28 lines, mechanical; full reviewer chain would have been ceremony. Verified by direct gates run + diff inspection. |
| `unitAI-w0h7z` | Investigate pi 0.70.x CLI changes; unpin pi-coding-agent in image | — | direct | Root cause: pi 0.70.x parser rejects the `--` option terminator. Fix: dropped `args.push('--', prompt)` → `args.push(prompt)` in `src/specialist/script-runner.ts:192`. Both 0.64 and 0.70.2 accept positional prompt. Image unpinned to `@latest`. End-to-end container smoke (pi 0.70.2 + nano-gpt/kimi) returned valid JSON. |
| `unitAI-6qctn` | sp script CLI — one-shot peer to sp serve for cron/scripts | executor `d0e569` (gpt-5.4-mini) | impl | New `src/cli/script.ts` (127 LOC) reuses `runScriptSpecialist` directly. Cron-friendly exit codes (0/1/2/3/4/5/6/7/75); `--single-instance` uses `flock` with EX_TEMPFAIL on contention. 3 unit + 2 integration tests pass. |
| `unitAI-dw58i` | Review sp script CLI implementation | reviewer `ffffc9` (gpt-5.3-codex) | review | PARTIAL — all 5 functional requirements MET. Process-only gaps: GitNexus audit trail and stale doc anchor `§10.2`. Both addressed in `unitAI-8nvx8`. |
| `unitAI-8nvx8` | Doc: sp script CLI exit codes section | — | direct | Added "CLI peer (`sp script`)" section + exit code table to `docs/specialists-service.md` matching `mapExitCode` mapping. |
| `unitAI-nsru6` | CI: build image with latest pi, smoke-test, fail loud on regression | — | direct | `.github/workflows/pi-compat.yml` — weekly cron + PR-trigger on `Dockerfile`/`script-runner.ts`/`session.ts` + manual dispatch. 4 smokes (no quota, no secrets, no LLM): script-runner flag presence, RPC-session flag presence, real positional-prompt acceptance with fake/fake model, container build + `/healthz`. Verified locally against pi 0.70.2. |
| `unitAI-t9t11` | PoC: drop mercury-atomic-summarizer schema-target spec into docs/examples | — | direct | `docs/examples/mercury-atomic-summarizer.specialist.json` matches inline JSON in migration doc § Schema target. Cross-link added. End-to-end smoke via `sp script` + real pi 0.70.2 + nano-gpt/kimi-k2.5: `success:true`, `parsed_json` valid against `output_schema {summary, controlled_tags}`. Schema-target table validated. |
| `unitAI-rfjbd` | Rename sp script --user-dir to --project-dir | — | direct | The flag is project root, not a user-spec dir. Renamed in `sp script` and `sp serve` (and `ScriptRunnerOptions.projectDir`); `--user-dir` kept as deprecated alias. New unit test asserts both flags resolve identically. |
| `unitAI-s2won` | docs/examples/specialists_client.py — reference Python adapter | — | direct | `docs/examples/specialists_client.py` — stdlib-only, ~165 LOC. `SpecialistErrorType` enum mirrors the closed `error_type` union with `TRANSPORT` for caller-side HTTP failures. Live round-trip via real `sp serve` + nano-gpt/kimi: valid `parsed_json` + `trace_id`. |
| `unitAI-bahj1` | sp validate --target script — pre-deploy compatGuard validator | executor `bfca58` (gpt-5.4-mini) | impl | `sp validate <path> --target script` runs schema parse + `compatGuard` offline. PASS prints one line; FAIL exits 1 with structured error. 4 unit tests pass. Manual smoke: `mercury-atomic-summarizer` PASS, deliberately-broken spec → `compatGuard: permission_required` exit 1. |

### Schema discipline — drift discovery, sweep, strict cut

| ID | Title | Specialist | Wave | Close Reason |
|---|---|---|---|---|
| `unitAI-826wl` | Fix specialists-creator JSON corruption + audit drift findings | explorers `35c107`/`60f59b`/`0ec693` (kimi-k2.5) | audit + fix | Three parallel explorer audits surfaced 13 issues. Fixed 10 mechanical: zsh prompt artifact in JSON line 18, stale skill path, `--preset standard` → `--preset medium`, stale `skills/specialist-author/scripts/` → `config/skills/specialists-creator/scripts/`, `communication.publishes` formalized in `CommunicationSchema`, `max_retries` claim clarified, `requires_worktree` + `auto_commit` added to authoring.md execution table. |
| `unitAI-yb9qu` | Schema uniformity follow-ups: output_to → output_file, diagnostic_scripts formalization | — | sweep | Initial pass only addressed what 3 explorers sampled — they covered 3 of 26 specs. Full sweep found `xt-merge.communication.output_to` was a typo'd dead alias hiding the silent drop of top-level `output_file` (consumed at `runner.ts:1310`). Migrated. `diagnostic_scripts` formalized in `CapabilitiesSchema`. New audit script committed at `config/skills/specialists-creator/scripts/audit-spec-uniformity.mjs`. Final: 26 specs · 0 parse errors · 0 unknown keys. |
| `unitAI-8n0aa` | Strict 1:1 schema cut: drop everything not consumed by runner | — | strict-cut | User direction: source of truth, no nice-to-have leftovers. Dropped 11 schema declarations (`CommunicationSchema` entirely, `diagnostic_scripts`, `normalize_template`, `examples`, `preferred_profile`, `approval_mode`, `metadata.author`/`created`, `heartbeat`, `ScriptEntry.path`). Stripped fields from 26 specs (`next_specialists` × 8, `publishes` × 10, `diagnostic_scripts` × 2, `created` × 2, `scripts.path` → `run` × 2). Updated `docs/authoring.md`, SKILL.md, `view.ts`, `scaffold-specialist.ts`, validate warnings. |

### Cleanup / closures (prior-session design beads + tracker)

| ID | Title | Close Reason |
|---|---|---|
| `unitAI-39bef` | Specialists-service v1.x — deferred items (epic tracker) | 4 children shipped this session (`xutg2`, `w0h7z`, `6qctn`, `nsru6`); 4 P3 children stay open as standalone deferred items gated on consumer demand. |
| `unitAI-v1g7z` | Evaluate specialists-service spec — production hardening | Design-only; output merged into `docs/specialists-service.md` + `docs/specialists-service-evaluation.md`. Implementation chain `unitAI-c6uvn` shipped. |
| `unitAI-hr6mm` | Explore specialists-service runtime surface area | Same — design merged, impl shipped. |
| `unitAI-nauhr` | Critique specialists-service spec — first-pass | Same. |
| `unitAI-f8hii` | Second-pass critique of specialists-service evaluation memo | Same. |
| `unitAI-vah49` | Weigh migration strategies for legacy script-specialists | Superseded by `docs/darth-feedor-migration.md` § Schema target. |
| `unitAI-vegyd` | Design legacy script-specialists migration plan | Superseded — same. |
| `unitAI-xjlt4` | Explore new specialists system surface for script/library use | Superseded — same. |

## Issues Filed (14)

Grouped by track.

### Filed-and-closed-in-session (chain artifacts)

| ID | P | Type | Title | Why |
|---|---|---|---|---|
| `unitAI-bx72n` | P2 | task | Review Zod schema passthrough fix | Reviewer-bead pattern for `unitAI-xutg2` chain. |
| `unitAI-dw58i` | P2 | task | Review sp script CLI implementation | Reviewer-bead pattern for `unitAI-6qctn`. |
| `unitAI-8nvx8` | P2 | chore | Doc: sp script CLI exit codes section | Reviewer flagged docs/specialists-service.md §10.2 didn't exist; this lands the section + exit code table. |
| `unitAI-rfjbd` | P3 | chore | Rename sp script --user-dir to --project-dir | Discovered when smoking the mercury PoC — `--user-dir` is the project root, not a user-spec dir. Confusing for migrators. |
| `unitAI-t9t11` | P2 | chore | PoC: drop mercury-atomic-summarizer schema-target spec into docs/examples | Migration doc shows the JSON inline; this lands the canonical Phase 1 spec as a copyable file with cross-link. |
| `unitAI-s2won` | P2 | feature | docs/examples/specialists_client.py — reference Python adapter | Phase 1 step 1 of darth-feedor migration specified the API but left implementation to darth-feedor. Reference removes design ambiguity. |
| `unitAI-bahj1` | P2 | feature | sp validate --target script — pre-deploy compatGuard validator | Migrators had no offline way to verify a spec before deploying — had to start `sp serve` and POST to find errors. |
| `unitAI-826wl` | P0 | bug | Fix specialists-creator JSON corruption + audit drift findings | Three audit explorers surfaced 13 issues including critical zsh prompt artifact in JSON. |
| `unitAI-yb9qu` | P2 | bug | Schema uniformity follow-ups: output_to → output_file, diagnostic_scripts formalization | Initial fix pass only addressed sampled specs; full sweep found two more drift items hidden by `.passthrough()`. |
| `unitAI-8n0aa` | P1 | chore | Strict 1:1 schema cut: drop everything not consumed by runner | User direction after seeing the audit results — source-of-truth principle. |

### Filed and still open (audit + bug surfaces)

| ID | P | Type | Title | Why |
|---|---|---|---|---|
| `unitAI-wdb2s` | P2 | task | Audit specialists configs + authoring docs vs current Zod schema | Parent audit bead; the explorer chain produced findings that have been closed via `unitAI-826wl` + `unitAI-yb9qu` + `unitAI-8n0aa`. Bead can be closed at next session if user agrees. |
| `unitAI-9ilgw` | P2 | task | Audit: specialists-creator skill + JSON deep correctness check | Same; closed effectively. |
| `unitAI-vt0my` | P2 | task | Audit: docs/authoring.md drift vs current schema/runtime | Same; closed effectively. |
| `unitAI-1ymr8` | P3 | bug | tests/unit/cli/help.test.ts is broken under node-based vitest | Pre-existing — vitest spawns node, dist needs bun. 9 help tests fail with `__require is not a function`. Filed during help-fix work; not regression from this session. |

## Specialist Dispatches

### Wave summary

| Wave | Specialists | Models | Outcome |
|---|---|---|---|
| Zod passthrough impl | executor `3cfa97` | gpt-5.4-mini | DIED mid-vitest (process-level death, not bug). Diff was complete and clean (28 LOC, 1 new test). Orchestrator ran gates manually + merged. |
| sp script impl | executor `d0e569` | gpt-5.4-mini | DONE — 336 LOC across 6 files. Followed contract; 5 tests pass. Lingering `waiting` state until cleaned up at `sp ps` review. |
| sp script review | reviewer `ffffc9` | gpt-5.3-codex | DONE — verdict PARTIAL with 2 process gaps (no functional defects). Fix bead opened (`unitAI-8nvx8`). |
| sp validate impl | executor `bfca58` | gpt-5.4-mini | DONE — 4 tests pass. Worktree was branched before Python adapter merge so required rebase before FF-merge. |
| Audit (3-way parallel) | explorer `35c107`, `60f59b`, `0ec693` | nano-gpt/moonshotai/kimi-k2.5 (×3) | DONE — surfaced 13 findings between them. **Coverage gap**: only sampled 3 of 26 specs; the comprehensive sweep that I ran afterwards found 2 additional drift items. |

### Problems sub-table

| Problem | Resolution |
|---|---|
| Executor `3cfa97` (Zod) died mid-`bunx vitest`; status reported "running" then "dead" with no `result` | Stopped the dead job; ran gates manually; verified 28-line diff + 1 new test independently; closed PASS. |
| Reviewer `ffffc9` `sp result` was unavailable mid-run because the executor `d0e569` was still in `waiting` state | Reviewer fell back to `bd show --json` + `git diff master..HEAD` for evidence; saved memory `reviewer-cannot-rely-on-specialists-jobs-in-worktree` (its own observation). Verdict still produced with full requirement-coverage matrix. |
| Audit explorers (`35c107`/`60f59b`/`0ec693`) sampled only 3 of 26 specs; produced false positives (`mandatory_rules` and `output_file` flagged as unknown when both ARE in schema) | Wrote a comprehensive grep-based audit script at `config/skills/specialists-creator/scripts/audit-spec-uniformity.mjs`; ran direct sweep; found the 2 real drift items the explorers missed. |
| Initial `mapPermissionToTools` regex grep flagged `extensions.serena`/`gitnexus` as DEAD when they're consumed at `runner.ts:889-890` (regex too narrow) | Followed up with looser greps + manual verification; the schema↔runtime audit ended with no false negatives. |

## Problems Encountered

| Problem | Root Cause | Resolution |
|---|---|---|
| Pi 0.70.x rejects `--` separator with `Unknown option: --` | Pi 0.70 changed/removed the option-terminator semantics. | Dropped `args.push('--', prompt)` → `args.push(prompt)` in `src/specialist/script-runner.ts:192`. Both 0.64 and 0.70.2 accept positional prompt. Image unpinned to `@latest`. |
| `xt-merge` specialist has been silently failing to write `merge-prs-result.md` to disk | `communication.output_to` is a typo'd dead alias. The schema's consumed field is top-level `specialist.output_file` (`runner.ts:1310`). `.passthrough()` let the wrong key survive without warning. | Migrated `output_to` → `output_file` at the correct nesting in canonical + mirror. |
| `config/specialists/specialists-creator.specialist.json:18` had `"permission_requiredspecialists — zsh "` as a JSON key | Operator's terminal prompt text was pasted into the file at edit time. `.passthrough()` accepted it; no schema warning fired. | Renamed to `"permission_required"`. Spec now parses cleanly. |
| 19 stale `.xtrm/skills/active/pi/<name>/` skill paths across canonical + mirror specs | The `pi/` subdirectory was removed/renamed in a prior layout migration; these refs were never swept. The actual layout is `.xtrm/skills/active/<name>/` (each is a symlink to `../default/<name>`). | Bulk-replaced via `sed` across 9 files; mirror sync from canonical for residual model-string drift in `memory-processor`/`researcher`/`sync-docs`. |
| Migration doc Phase 3 told darth-feedor to use `template: "normalize_template"` from Python — but the script-runner reads `input.template` as literal template TEXT, not a key-name lookup. Plus `normalize_template` was just dropped from the schema. | The migration doc was written aspirationally; alternate-template lookup was never implemented. | Migration doc updated: analyst stage now ships **two** specialist files. Python adapter `template` parameter clarified as override-text only. |
| `git add config/specialists/*.specialist.json .specialists/default/*.specialist.json` did NOT pick up `parallel-review.specialist.json` deletions because the glob only matches existing files | Bash glob expansion behavior — `*.specialist.json` evaluates against the working tree, not the index. Deletions remained as un-staged worktree changes. | Explicit `git rm` for the two parallel-review files in a separate commit. |
| Quality-check hook (`.xtrm/hooks/quality-check.cjs`) intermittently blocks `Edit` tool with "No stderr output" | Pre-existing transient hook noise (not from this session's changes). Hook returns no useful diagnostic when it fires. | Acknowledged each instance; continued; verified independently via `npx tsc --noEmit` and `vitest run` that no actual regressions were introduced. |
| `tests/unit/cli/help.test.ts` (9 tests) fails under `bunx vitest` with `__require is not a function` | `vitest`'s test runner spawns `node` for `process.execPath`; the test does `execFileSync(process.execPath, [dist/index.js, 'help'])`; node can't execute the bun-built `dist/index.js` because it uses the `__require` shim. Same root cause that forced the runtime container to use `oven/bun:slim` instead of `node-slim` last session. | Filed `unitAI-1ymr8` and continued; not a regression introduced by this session's commits. |

## Code Changes

### Schema discipline (`src/specialist/schema.ts` — net −22 lines after the strict-cut)

Three sequential commits walked the schema through:
1. **f27c826c** — `.passthrough()` on every nested object so unknown keys survive parse (catches the `sp edit` round-trip drop bug).
2. **02deb96e** — formalized two real declarative metadata fields (`communication.publishes`, `capabilities.diagnostic_scripts`).
3. **68eddee6** — strict cut: dropped `CommunicationSchema` entirely, plus 10 other declarations the runner doesn't read. Validate-warning code for the deprecated fields removed in lockstep.

### Runtime (`src/specialist/script-runner.ts`, `src/cli/script.ts`, `src/cli/serve.ts`)

- **59d06437** — `args.push('--', prompt)` → `args.push(prompt)` (one line; pi 0.70.x compat).
- **2cbbaeed** — new `src/cli/script.ts` (127 LOC). Reuses `runScriptSpecialist` with no duplication; cron exit-code mapping; `flock` for `--single-instance`. Dispatch entry added in `src/index.ts`.
- **4ae134b8** — `--user-dir` → `--project-dir` rename across `script.ts`, `serve.ts`, `script-runner.ts` `ScriptRunnerOptions`. `--user-dir` retained as deprecated alias. Test asserts identical resolution.
- **4b591b28** — `src/cli/validate.ts` gains `--target=script` mode. Loads via file path, runs schema parse + `compatGuard`, structured exit codes.

### Image and CI (`Dockerfile`, `.github/workflows/pi-compat.yml`, `.gitignore`)

- **59d06437** — `Dockerfile` line 25 `@0.64.0` → `@latest`. `docs/specialists-service-install.md` pin guidance updated.
- **5077f9de** — new workflow file with weekly cron + PR-on-spawn-surface trigger + manual dispatch. 4 smoke steps. `Dockerfile` gains `ARG PI_VERSION=latest`. `.gitignore` no longer wholesale-excludes `.github/`.

### View / scaffold cleanup (`src/cli/view.ts`, `config/skills/specialists-creator/scripts/scaffold-specialist.ts`)

- View stopped rendering `normalize_template` and `examples` blocks (dropped from schema).
- Scaffold's `DEAD_FIELDS` set emptied since the schema no longer carries them.

### Spec files (26 modified, 2 deleted)

Stripped: `next_specialists` × 8, `publishes` × 10, `diagnostic_scripts` × 2, `created` × 2, deprecated `path` migrated to `run` × 2, empty `communication`/`capabilities` containers collapsed. Stale skill paths swept × 9. Mirror sync for residual model-string drift × 4. `parallel-review.specialist.json` removed from both canonical and mirror.

### Tests

| File | Change |
|---|---|
| `tests/unit/specialist/schema-passthrough.test.ts` | New (1 test, 55 LOC) |
| `tests/unit/cli/script.test.ts` | New (3 tests, 73 LOC) |
| `tests/integration/sp-script.test.ts` | New (2 tests, 111 LOC) |
| `tests/unit/cli/validate.test.ts` | +1 test for `--target script` PASS, +1 for compatGuard FAIL |
| `tests/unit/cli/script.test.ts` | +1 test asserting `--user-dir` and `--project-dir` resolve identically |

Final: all 8 directly-affected tests pass; 13 unrelated pre-existing failures unchanged.

## Documentation Updates

| Doc | Status | Change |
|---|---|---|
| `handoff-feedor.md` | new (one page) | Operator handoff at repo root pointing at every artifact darth-feedor needs. Phases 1–5 split + locked decisions + open beads. |
| `docs/specialists-service.md` | +20 lines | New "CLI peer (`sp script`)" section with exit code table; `--project-dir` documented; `--user-dir` noted as deprecated alias. |
| `docs/specialists-service-install.md` | edited | Image pin guidance updated (`@latest` tracking). |
| `docs/darth-feedor-migration.md` | edited | Phase 3 multi-stage analyst now ships **two** specs (alternate template lookup never worked + schema dropped). Translation table updated. Cross-link to `docs/examples/mercury-atomic-summarizer.specialist.json`. Cross-link to `docs/examples/specialists_client.py`. |
| `docs/authoring.md` | net edits | Added `requires_worktree` + `auto_commit` to execution table; clarified `max_retries` ignored by script-class; dropped `communication` section, dropped deprecated alias for `skills.scripts.path`, trimmed dead optional fields. |
| `docs/examples/mercury-atomic-summarizer.specialist.json` | new | Canonical Phase 1 spec; live-smoked via `sp script` + nano-gpt/kimi. |
| `docs/examples/specialists_client.py` | new + edited | Reference Python adapter, then `template` param clarified as override-text only after schema strict-cut. |
| `config/skills/specialists-creator/SKILL.md` | edited | Fixed `--preset standard` → `--preset medium` (×2); fixed stale `skills/specialist-author/scripts/` path (×3); dropped communication section; dropped deprecated alias mention; updated full spec example. |
| `config/skills/specialists-creator/scripts/audit-spec-uniformity.mjs` | new | Portable audit script (run from any cwd via bun); reports parse failures + unknown keys; KNOWN sets in lockstep with schema. |
| `src/cli/help.ts` | +2 entries | `sp serve` and `sp script` surfaced in core commands. |

## Open Issues with Context

### Ready for next session — confirm-and-close audit umbrellas

| ID | P | Title | Context / Suggestions |
|---|---|---|---|
| `unitAI-wdb2s` | P2 | Audit specialists configs + authoring docs vs current Zod schema | All findings from the 3 audit explorers were addressed via `unitAI-826wl`, `unitAI-yb9qu`, and `unitAI-8n0aa`. Plus the strict-cut commit `68eddee6` formalized everything. Bead can close with reason "Findings addressed across the three follow-up beads; schema is now strictly 1:1 with runner; audit script committed at `config/skills/specialists-creator/scripts/audit-spec-uniformity.mjs`". |
| `unitAI-9ilgw` | P2 | Audit: specialists-creator skill + JSON deep correctness check | Same — rolled into `unitAI-826wl`. Close with pointer. |
| `unitAI-vt0my` | P2 | Audit: docs/authoring.md drift vs current schema/runtime | Same — `docs/authoring.md` updated in `b37d7e96` + `68eddee6`. Close with pointer. |

### Ready for next session — darth-feedor adoption (consumer side)

| ID | P | Title | Context / Suggestions |
|---|---|---|---|
| (no specialists-side work blocked) | — | — | All Phase 1 prep done. **Operator action**: copy `docs/examples/specialists_client.py` → darth-feedor `shared/specialists_client.py`; copy `docs/examples/mercury-atomic-summarizer.specialist.json` → darth-feedor `.specialists/user/`; build image from this repo on VPS (`docker/compose.example.yml`); smoke against staging. Then proceed Phase 2 → 5 per `docs/darth-feedor-migration.md`. Specialists-repo only changes if Phase 1 surfaces a real bug or Phase 3 needs a new primitive. |

### Backlog — gated on consumer demand

| ID | P | Title | Context / Suggestions |
|---|---|---|---|
| `unitAI-3k6sa` | P3 | sp serve --allow-skills / --allow-skills-roots / --allow-local-scripts | Trust flags for the currently-blocked `skills.paths` / `prompt.skill_inherit` fields. Default-off is correct (prompt-injection vector if multi-tenant). Implement only if a trusted single-tenant deployment requires it. |
| `unitAI-rw13n` | P3 | Programmatic runScript() export for Node consumers | Tiny: `package.json#exports` + re-export of the existing `runScriptSpecialist`. Implement only if a Node consumer actually needs in-process invocation. |
| `unitAI-daxxp` | P3 | sp serve /readyz endpoint with degraded states | Only matters if a consumer's deployment uses k8s-style readiness probes. Sidecar + `/healthz` is sufficient for the current darth-feedor topology. |
| `unitAI-or6kw` | P3 | sp serve hot-reload of specialist files | Restart works for now. Lowest priority. |
| `unitAI-1ymr8` | P3 | tests/unit/cli/help.test.ts is broken under node-based vitest (dist needs bun) | Pre-existing; 9 help tests fail with `__require is not a function`. Fix is small: spawn `bun` explicitly via `which bun` inside the test, OR rewrite to import `help.ts` directly and capture stdout. Doesn't block any feature work. |

### Pre-existing, untouched this session

These were already open on the board and represent the parallel SQLite-observability track or unrelated work:

| ID | P | Title | Note |
|---|---|---|---|
| `unitAI-ctkk9` | P0 | P0: fix reviewer traceability gaps for gitnexus impact verification and reviewed diff context | Pre-existing P0; orthogonal to specialists-service. |
| `unitAI-71lqc`, `unitAI-92b7d`, `unitAI-0tn47`, `unitAI-rodmt`, `unitAI-oyyt2`, `unitAI-frnbe`, `unitAI-grqex`, `unitAI-pnqgd`, `unitAI-2b3m`, `unitAI-t0spl`, `unitAI-c6he0`, `unitAI-0vb3`, `unitAI-9d3a`, `unitAI-231x`, `unitAI-zw9w1`, `unitAI-qsdt6`, `unitAI-brbb`, `unitAI-e90j`, `unitAI-n4w30` | various | various | Parallel fln4q-epic / observability remediation track. Not from this session. |

## Memories Saved

| Key | Content |
|---|---|
| `pi-0-70-x-dropped-the-option-terminator` | pi 0.70.x dropped `--` from CLI parser; prompt is purely positional. Fix: drop `args.push('--', prompt)` → `args.push(prompt)`. Both 0.64 and 0.70.2 accept positional prompt. Image now `@latest`. |
| `pi-compat-ci-strategy-smoke-checks-a-every` | CI smoke strategy: regex-grep every required spawn flag in `pi --help`, then real-spawn with `fake/fake` model — `Unknown option` is a regression, auth/provider error is fine. No quota, no secrets, no LLM. |
| `sp-script-cli-user-dir-flag-is-the` | `sp script --user-dir` was the project root, not a user-spec dir. Loader resolves `<projectDir>/.specialists/user/`. Live smoke confirmed migration-doc schema-target table is correct end-to-end against real pi. |
| `audit-script-detected-output-to-typo` | Spec uniformity audit script at `config/skills/specialists-creator/scripts/audit-spec-uniformity.mjs`. Walks every spec; flags unknown keys that survive `.passthrough()` silently. Caught `xt-merge.communication.output_to` (silent output_file drop) and `diagnostic_scripts` (declarative-only). KNOWN sets in script must stay in lockstep with `src/specialist/schema.ts`. |
| `schema-1-1-with-runner-enforced` | Schema is strictly 1:1 with runner. 11 declarations dropped from `src/specialist/schema.ts` (CommunicationSchema entirely; `diagnostic_scripts`; `normalize_template`/`examples`; `preferred_profile`/`approval_mode`; `metadata.author`/`created`; `heartbeat`; `ScriptEntry.path`). New invariant: every JSON field must map to a runtime consumer; new fields must be added with their consumer in the same commit. |

Existing memory used (not re-saved): `zod-specialistschema-strips-unknown-keys-on-parse-verified` from prior session — guided the Zod fix recipe.

## Suggested Next Priority

The bottleneck is now darth-feedor-side (operator work on the VPS). Specialists-repo work that could happen:

1. **(Cleanup, ~5 min)** Close `unitAI-wdb2s` / `unitAI-9ilgw` / `unitAI-vt0my` audit umbrellas with reasons pointing at the three follow-up commits. They're effectively done.
2. **(Optional, ~30 min)** Land `unitAI-1ymr8` so `bunx vitest` runs the help tests cleanly. Small, mechanical, useful for CI hygiene. Not blocking.
3. **(Defer)** Any of `unitAI-3k6sa`/`rw13n`/`daxxp`/`or6kw` — open until darth-feedor's Phase 2/3/4 surfaces a real need.

The right move from here is to switch hosts and start Phase 1 in darth-feedor: copy the two artifacts from `docs/examples/`, build the image on the VPS, smoke against one real article through `mercury-atomic-summarizer`. Specialists repo only changes if that smoke fails or a Phase 3 primitive is missing.

### Decision points the next agent should not re-litigate

1. **Strict 1:1 schema↔runtime is the invariant**: any new JSON field must come with a runtime consumer in the same commit. `.passthrough()` exists to preserve forward-compat, not to harbor declarative-only fields.
2. **Build-from-repo is the install path** for darth-feedor — registry publishing deferred.
3. **Pi creds via `~/.pi` mount**, never via `--api-key` flags.
4. **Multi-tenant sidecar is unsupported** in v1; one container per consumer.
5. **`mapPermissionToTools` explicitly enables natives** (`read,bash,edit,write,grep,find,ls`) — Serena is a peer, not an enforcement layer in specialist runs. Doc-prescribed Serena-first workflow is aspirational. Operator decision pending on whether to tighten the allowlist.
